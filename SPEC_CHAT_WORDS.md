# ТЗ чату «Слова» — анаграммы и филворды

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная · 06.09.2026*

> 🔴 **ПРАВИЛА — В `CHATS_RULES.md`, И ОН ГЛАВНЕЕ ЭТОГО ФАЙЛА.**
> Здесь они пересказаны для холодного старта; при расхождении верен
> `~/dev/psygames/CHATS_RULES.md`. Правило меняется ТАМ и только там — иначе
> девять копий разъедутся, а на молча расходящихся копиях проект уже обжигался.
>
> **Первым делом — §0 того файла:** регистрация в TeamOps (иначе тебя нет на
> доске и задачи не дойдут), своя папка со своим `PROJECT_REF.md`, и только
> потом код.


Ты ведёшь один раздел ВДОЛГУЮ. Это не разовая задача, а хозяйство: словесные
режимы PsyGames — анаграммы (четыре режима) и филворды.

---

## 1. Твои файлы

```
frontend/src/games/anagrams/**
frontend/src/games/fillwords/**
frontend/src/components/letterWheel/**
frontend/app/games/anagrams.tsx
frontend/app/games/proofreading.tsx
frontend/src/constants/allWords*.json
wordlist-build/**
frontend/src/__tests__/  — только anagram*, allwords*, fillwords*, proofread*, wordlist*
```

**За их пределы не выходишь.** Особенно не трогаешь `src/games/dots-connect/`,
`src/games/water-sort/`, `app/games/goods-sort.tsx` — там работают другие.

**Общий слой — через координатора**, сам не правишь:
`src/constants/games.ts` · `profiles.ts` · `hubContents.ts` · `helpMap.ts` ·
`whatsNew.ts` · `CHANGELOG.md` · версии в трёх манифестах · метки git.

Словари (`LanguageContext.tsx`, `translations/*.ts`) — **только свои новые
ключи**. Чужие строки не переставляешь и не переводишь заново.

---

## 2. Правила работы — постоянные

- 🔴 **`git add -A` запрещён.** Только `git add <свои файлы>`. 06.09.2026
  из-за подметания дерева работа трёх сессий уехала в чужие коммиты с чужими
  заголовками.
- `git pull --rebase` перед правкой, `git push` сразу после коммита.
- Коммит после КАЖДОГО куска, а не в конце дня. Подпись `[Claude·MAC]`.
- Версию не поднимаешь, метку не ставишь, выпуск не собираешь.
- Агентов не запускаешь: работа умещается в свои файлы.

**Каждая правка несёт три вещи:**

1. **Замер ДО числом.** «Кажется, мелко» — не замер. Меряй ту сущность, что
   названа в задаче, а не соседнюю: 05.09 пул «детского мата» мерился по ПОЛЮ
   мата, а вывод писался про УЗОР — совпало у всех, узором оказалась треть.
2. **Гейт, который краснеет на состоянии ДО.** Гейт, ни разу не покрасневший,
   не стережёт ничего.
3. **Мутация в обе стороны.** Сломал — покраснел; вернул — позеленел.

**Лестницу трудности прогоняй ЦЕЛИКОМ по уровням.** Не сравнивай первый с
последним: ломается она на стыках участков. За 6 сентября так нашлось трижды —
филворды выдавали одно и то же поле с 13-го уровня по 500-й, переливалка на
одиннадцатом была легче десятого, «Найди все слова» не имели лестницы вовсе.

**Отказ требует такого же замера, что и согласие.** Под «не подходит» — чем
померил, что вышло, почему это «нет», и отдельно: что из отклонённого ВСЁ РАВНО
переносится.

**Разборы уже написаны — читай, не мерь заново:** `REF_WORDS.md`,
`WORDLIST_RESEARCH.md`, `WORDLIST_NONLATIN.md`.

---

## 3. Что уже сделано — не переделывай

| | где |
|---|---|
| Четыре режима анаграмм: классика, «Квадрат слов», «Все слова», «Кроссворд» | `app/games/anagrams.tsx` |
| Круг букв для ввода | `src/components/letterWheel/` |
| Укладчик кроссворда на TS, считает на лету, зерно от уровня | `src/games/anagrams/core/crossword.ts` |
| Семь языков в «Найди все слова» (ru, en, de, es, fr, it, pt) | `src/constants/allWords*.json` |
| Лестница наборов: паки отдаются по возрастанию числа слов | `core/allWords.ts`, функция `лестница()` |
| Гейт на взрослую лексику, один на все языки | `__tests__/wordlist-adult-free.test.ts` |
| Филворды прямоугольные, до 12×9, 11 форм за 60 уровней | `fillwords/core/generator.ts` |
| Список искомых слов в филвордах | `app/games/proofreading.tsx` |

⚠️ **Найденную MIT-библиотеку укладки кроссворда не подключаем.** Своё уже
работает и закрыто гейтом на правилах криптоворда; замена — новая зависимость
ради того же результата. Впиши ссылку в `REF_WORDS.md` как «рассмотрено и
отклонено, потому что своё работает» — с замером, а не молча.

---

## 4. Очередь — сверху вниз

### 4.1. Филворды до пяти тысяч уровней
У образца жанра их 5000, у нас лестница упирается в 12×9 на 24-м уровне и
дальше уровни отличаются только раскладом. Замерь, чем растить дальше: третья
ось (тема слов? запрет диагоналей? лимит времени?) или больше форм поля.
Ответ дай числом, а не списком идей.

### 4.2. Лестница «Квадрата слов»
Замер 06.09: у режима её нет — кольцо выбирается остатком от номера уровня,
трудность не растёт. Сделай осью то, что правда меняет работу, и докажи
прогоном всех уровней.

### 4.3. Кроссворд на семи языках
Сейчас гейт проверяет укладку на ru, en, de, fr. Прогони по es, it, pt и
приведи долю раскладок, влезающих в телефонную коробку. Ниже 85% — скажи
прямо и предложи, чем добрать.

### 4.4. Нелатинские языки
`WORDLIST_NONLATIN.md` говорит: корейский переносится начисто (100% слов
собираются обратно из чамо), арабский с налогом на начертание, японский на
кане, хинди вдвое тоньше, китайскому механика не подходит по числу. Возьми
корейский первым — он самый чистый.

---

## 5. Готово — это когда

По каждому пункту есть: число ДО и число ПОСЛЕ · гейт, краснеющий на состоянии
ДО · мутация в обе стороны, прогнанная и описанная · `npx tsc --noEmit` без
ошибок · свой набор проб зелёный · коммит с подписью.

Полный `npx jest` и `node scripts/lint-ratchet.mjs` — один раз в конце.
Ратчет обязан сказать «Долг не вырос»; если вырос — чини СВОИ замечания, а не
поднимай потолок.

---

## 8. Своя папка и свой `PROJECT_REF`

🔴 **Первое, что делаешь — заводишь себе папку.** Файлы раздела не сыплются в
корень: там уже 220+ файлов и найти ничего нельзя.

```
~/Downloads/Code claude/psygames/<ИМЯ>-chat/
    PROJECT_REF.md      ← твой журнал, ведёшь сам
    art/                ← листы картинок, нарезка, контактные листы
```

Соседи уже так живут: `words-chat/`, `sorting-chat/`. Прежде чем создавать —
загляни, нет ли уже твоей папки.

**Что в твоём `PROJECT_REF.md`** (образец — у соседей, форма та же):

- ★ строка на каждый заход: дата, подпись `Claude·MAC`, что сделано, каким
  ЗАМЕРОМ доказано. Числа обязательны: «было 4,2 клетки на путь, стало 6,4», а
  не «стало лучше».
- 🔴 отдельной строкой — каждый найденный дефект и то, чем он был НЕВИДИМ.
- 🚫 отказы: чем мерил, что получилось, что из отклонённого переносится.
- Открытые вопросы к Денису — списком, чтобы он отвечал одним словом.

⚠️ **`PROJECT_REF.md` в корне репозитория — не твой.** Он у координатора и
лежит в `.gitignore` как коммерческая тайна. Твой — в твоей папке.

---

## 9. Картинки: как генерировать и как нарезать

### 9.1. Правила рисования

- 🔴 **Реалистично.** Объём, материал, свет — даже мультяшному предмету. Плоские
  заливки Денис бракует.
- 🔴 **Сетка листа фиксирована: 7 × 8.** Не 4×5, не 3×3 — семь колонок, восемь
  рядов. Спрашивать не нужно, это решено.
- Есть образец — **описывай ПОЗЫ, а не внешность.** Модель и так видит, как
  предмет выглядит; описание внешности поверх образца ломает сходство.
- Геометрию задавай ЧИСЛОМ: «три кольца, зазор в четверть диаметра», а не
  «чтобы не сливались» — иначе модель сливает.
- ⚠️ Модель отказывается рисовать похожих на киногероев. Отказ выглядит
  техническим, причина правовая — переформулируй, не долби.

### 9.2. Чем генерировать

MCP `kie` → `generate_image` (Nano Banana 2). Один лист сеткой — дешевле и
ровнее, чем 56 отдельных вызовов: **одна генерация 7×8, потом нарезка**.

```
mcp__kie__generate_image
  prompt: "<сетка 7×8, ряд = состояние, ровный серый фон, объекты НЕ отбрасывают тень>"
  resolution: "4K"
  output_dir: "~/Downloads/Code claude/psygames/<ИМЯ>-chat/art"
```

🔴 **«Объекты не отбрасывают тень» — обязательный пункт промпта.** Иначе модель
добавит контактное пятно сама, а вырезание фона его не снимет: тень темнее
фона, заливка от края её не берёт, а снятие тени объедает нижнюю кромку самого
предмета. Оплачено дважды — лист гаек пришлось перерисовывать.

### 9.3. Чем нарезать и вырезать фон

Нарезка листа — PIL, по числу колонок и рядов (готовый рецепт лежит у соседей
в `words-chat/`, лист `words-thumbs-sheet-7x8.jpg` и папка `thumbs-cut`).

Вырезание фона — **скилл `bg-cutout`**, не руками:

```
python scripts/cutout.py "art/*.png" --out art/cut/ --contact-sheet
```

- По умолчанию `rembg` моделью `isnet-anime` — она под рисованное с обводкой.
- 🔴 **Контактный лист обязателен.** На нём брак виден сразу; без него бледный
  предмет уезжает в игру пустым.
- Бледный предмет пропал → `--method colorkey` (заливка от края; на плоском
  фоне точна независимо от бледности).
- Серый призрак тени → `--flatten-alpha 170`.
- Плотная тень пережила → `--strip-offset-shadow`.
- ⚠️ На светлом предмете (мятный, белый) заливка с числовым допуском съедает
  низ — у скилла допуск считается от самого изображения, поэтому зови скилл, а
  не свой скрипт.

### 9.4. Куда класть готовое

Вырезанные `.webp` — в `frontend/assets/images/<твой раздел>/`, карта
`require()` — **сгенерированным** файлом (`*.generated.ts`) со скриптом
сборки рядом. ⚠️ Шапка «собрано скриптом» обязана называть СУЩЕСТВУЮЩИЙ
скрипт: 06.09.2026 карта гаек звала `build-nut-styles.mjs`, которого не было,
и следующий заход правил бы «сгенерированный» файл руками.
