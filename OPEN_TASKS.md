# Открытые хвосты PsyGames — что обозначено и НЕ сделано

> Заведён 04.09.2026 по просьбе Дениса: «список в реф-файл зафиксируй, что мы с тобой
> по задачам обозначили и не сделано ещё, чтобы потом в контексте не потерялось».
>
> ⚠️ Это НЕ дубль `project_tasks` в Supabase. Здесь то, что всплыло в разговоре и в
> отчётах и пока не заведено задачей либо заведено, но упирается в решение владельца.
> Закрытое отсюда УДАЛЯТЬ, а не помечать — иначе файл станет кладбищем.

## 🔴 Ждут решения Дениса

| Что | Суть | Чем упирается |
|---|---|---|
| Развилки в общий доступ | Девять новых развилок видны ТОЛЬКО профилю `nzt48` (`previewIn: ['nzt48']` в `games.ts`). НЗТ-48 — личный полигон Дениса, отданный в общий доступ на время разработки. | Проверить самому → сказать «включить всем» (убрать `previewIn`) или «фильтровать по профилям» |
| Дубликаты экранов торможения | `app/games/go-no-go.tsx` (429 строк) и `app/games/stop-signal.tsx` (583) — дубликаты режимов внутри `/games/inhibition`. Из интерфейса до них не дойти; `go-no-go` держится ссылкой из пресета зарядки. | Сносить или оставить. Записаны долгом в `merged-games-reachable.test.ts` |
| «Гимнастика глаз» вне сетки | `eye_gym` скрыт из каталога (`hideFromMenu`), обещанная кнопка на главной слита в «Паузу» 26.08. В сетке НЗТ упражнения нет — правило «в НЗТ видно всё» держится только через «Паузу». | Вернуть карточку (снять `hideFromMenu`) или оставить как есть |
| Три вопроса от 29.08 | Задача `7897a430`: предел решаемости, приёмка зарядки секундомером, единый движок плейлистов | Код готов или ждёт решения |
| Этап 4 геймификации | Задача `ade9a298`: расходуемые способности, сравнение с друзьями, 8 скриншотов Play, ключевые слова, ночная тема | Все пять — решения |
| Будильник на Windows | Задача `3119410d`: проверить запуск. ⚠️ `.msi` кросс-сборкой не получить — WiX только под Windows | Нужна машина Дениса |

## 🟡 Заведено, делается / очередь

| Что | Где | Состояние |
|---|---|---|
| Озвучка 4 аудио-игр | `a382fd2f` | генерация семплов Silero |
| Каркас приложений | `5c95a291` | шаблон Tauri + чек-лист публикации на четыре продукта |
| Retro Games | `82258af9` | ТГ Mini App, 21 игра |
| Хвосты 26.08 | `ad1f756a` | слияние «Дыхания» и «Глаз», 12 режимов |
| Кадры маскота | `00218752` | ждёт celebrate/eat от маскот-сервиса |
| Перевод «Паузы» | `b019f547` | 268 из 339 реплик |

## 🔵 Найдено замером, задачей не заведено

| Находка | Замер | Почему важно |
|---|---|---|
| Перерезать спрайт кота на зелёном фоне | Дыры внутри силуэта залечены (пак 1.0.4, 34 525 → 0), но выеденный КРАЙ — зазубрины ушей, срезанные кончики лап — заплаткой не восстановить | Нужна перерезка `--chroma 00b140`; исходного ролика/листа в репо нет |
| Гейт `sudoku-geometry-from-solution` изредка краснеет под нагрузкой | Замер 04.09: три прогона в одиночку зелёные, в полном прогоне упал 1 раз из 3. Тот же корень, что у уже починенного `sudoku-geometric-unique` — доска генерируется без зерна, и трудность её случайна | Сообщение поймать не удалось; чинить по тексту из CI, когда повторится |
| Анимации «ест» у питомца нет | Отчёт `ca24df45`: «угостить надо дорисовывать анимацию как он жрёт» | Кнопка «Угостить» есть, отклика нет |
| Развернуть статистику | Отчёт `0660eb0a`: «сказать, где молодец и где провал, дать тенденцию против вчера и рекомендации, в свёрнутом виде» | Экран статистики есть, разбора нет |
| Технология стикеров ТГ для эмоций | Отчёт `836df9a7` | Идея на изучение: чем рисуют анимированные стикеры и хватит ли этого на эмоциональный ряд питомца |
| Полоса и пустота между разделами | Отчёт `d2375dbe`, кадр главной | ⚠️ НЕ ВОСПРОИЗВЕДЕНО: на рендере 430×960 и в WebKit 403×873 полосы нет. Нужен точный указатель Дениса, что за элемент |
| Флэнкер: «тулбар в две строчки, экран уезжает» | Отчёт `2501242d`, iPhone 403×873 | ⚠️ ЧАСТИЧНО НЕ ВОСПРОИЗВЕДЕНО: тулбар в одну строку, карточка стимула стоит намертво (6 проб — одна позиция). Замер видел разовое переполнение картинкой 456 px при 403 — поймать не удалось |
| Лестница судоку обещает 80, а содержания на 92 | Замер 03.09 | Карта уровней врёт на 12 ступеней |

## Правила, которые НЕЛЬЗЯ потерять

1. **В профиле НЗТ-48 доступно КАЖДОЕ упражнение.** Слова Дениса 04.09: «главное не проеби правило». Держится гейтом `nzt-shows-everything.test.ts` — он ходит по ссылкам из развилок и падает, если игра стала недостижимой.
2. **Развилка — своё.** Хаб показывает свой состав целиком; фильтровать его содержимое по профилю Денис запретил 04.09.
3. **Имена развилок выводятся из каталога**, а не переписываются в гейты: `src/__tests__/_helpers/hubScreens.ts`.
