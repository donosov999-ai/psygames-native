<!-- SPEC_SCREEN_GEOMETRY.md · координатор · 11.09.2026 · дополнен 17.09.2026 -->
# Геометрия игрового экрана: один канон и кто от него отходит

> 📐 **ЭТО ОТЧЁТ ЗАМЕРА, А НЕ ЗАКОН.** Правила расположения — где стоят шапка,
> полоса показателей, поле, низ и стрелки, и почему именно так — живут отдельно:
> `UI_LAYOUT_RULES.md`. Здесь только состояние на дату: кто на каноне, кто отходит
> и по какой причине. Числа берутся из `frontend/src/components/gameLayout.ts`.

Денис 11.09.2026: «дизайн плавает, центры квадратов особенно: то там по высоте, то там,
то шире то уже — надо как-то к единому стандарту прийти». И следом: «доска всегда внутри
одного и того же квадрата — давно уже должен был это сделать для ВСЕХ приложений».

Стандарт задан **кодом, а не договорённостью**: `frontend/src/components/gameLayout.ts`.
Разделам его не переписывать — брать оттуда. Свои 118 или 124 разводят разделы между
собой, и скачок возвращается, только реже.

## Канон

| величина | сколько | что это |
|---|---|---|
| `ШАПКА` | 58 | строка названия, паузы и питомца |
| `ПОЛОСА_ПОКАЗАТЕЛЕЙ` | 61 | плашка счётчиков; держит высоту, даже когда счётчиков мало |
| `ВЕРХ_ПОЛЯ` | **119** | = ШАПКА + ПОЛОСА. Отсюда начинается поле игры |
| `РЯД_ДЕЙСТВИЙ` | 56 | служебный ряд значков ПОД полем (отмена, подсказка, перемешать) |
| `ВЫСОТА_РЯДА_СЛУЖЕБНЫХ` | 61 | прибитый ряд под полем в `GameShell`: 8 + 48 + PAD_V (17.09.2026) |
| `ПОЛОСА_ОТВЕТА` | 120 | нижняя полоса, когда ответ даётся кнопками |
| `ПАЛЕЦ` | 48 | минимальная сторона нажимаемого |

Правило низа записано в самом каркасе (`GameShell`, пропы `bottom` и `headerActions`):
**низ принадлежит ОТВЕТУ; служебное — одним рядом значков сразу ПОД полем** (решение Дениса
17.09.2026). Над полем служебного ряда нет ни у кого, поэтому поле у всех начинается с 119;
слот над полем остался только у плейлиста (`frame`).

## Замер 11.09.2026: где мы на самом деле

Статическая сборка `main`, окно 390×844, прибор `frontend/scripts/screen-geometry.mjs`.

| | |
|---|---|
| экранов в сборке | 99 |
| измеримых (есть поле и полоса) | **85** |
| **верх поля ровно 119** | **63 из 85** |
| размах верха поля | 119 … 277 |
| размах высоты полосы | 61 … 141 (у 75 из 85 — ровно 61) |

Четырнадцать неизмеримых — это развилки (`*-hub`) и `span`: у них игрового поля нет
вовсе, и мерить там нечего. Это не дыра в замере.

## Замер 17.09.2026: служебный ряд под полем и что он задел

Статическая сборка ветки ряда (после `main` 9abfcac1), WebKit, окна 390×844 и 360×640, прибор
координатора `scratchpad/sim/aux-row-sweep.mjs` (все экраны игр, вход в партию):

| | до | после |
|---|---|---|
| служебное НАД полем | 33 экрана | **0** |
| служебное за краем окна | 1 | **0** |
| ряд служебных в 2+ строки | — | **0 из 35** на обоих окнах |
| «Сортировка товаров»: поле 360×640 | 398 | **460** (+62: ряд одной строкой) |

Что этот переезд задел и чем закрыто (замеры экспорта 17.09.2026):
· **Судоку** — резерв доски от высоты окна не знал про ряд под полем: второй ряд цифр под кнопками
  (384×784, низ клавиатуры на 44 ниже поля). Закрыто в 2.54.22: сторона доски от высоты поля, служебное
  значками — 384×784 доска 346, цифры внутри поля; 360×640 доска 256 вместо 202.
· **Головоломки** — пятая команда в ряду переносила его на 360–375 («Галактики»: доска −20, кнопка
  отзыва 673 px²) → переключатель значком; у режимов с крестовиной значки «про решение» — в её пустых
  клетках («Мосты»: 996 → 6 px²).
Правила, выведенные из этого, — в законе: `UI_LAYOUT_RULES.md`, правила 1, 9, 11 (полный экран), 12
(кнопка отзыва).

## Двадцать два отклонения и причина каждого

### Причина А — полоса показателей переносится на вторую строку (10 экранов)

Счётчиков столько, что ряд не влезает в ширину, `flexWrap` уносит лишнее вниз, и полоса
забирает у поля ровно столько, на сколько выросла.

| экран | верх поля | полоса | лишнее |
|---|---|---|---|
| `word-pairs` | 128 | 70 | +9 |
| `digit-span` | 132 | 74 | +13 |
| `picture-pairs` | 136 | 78 | +17 |
| `ant` · `mnemonics` | 138 | 80 | +19 |
| `targets` | 143 | 85 | +24 |
| `counter` | 145 | 87 | +26 |
| `sudoku-samurai` | 277 | 109 | +48 (и ряд над полем +110) |
| `phonemic-fluency` | 199 | **141** | **+80** |

Что делать владельцу: убрать из шапки счётчик, который не читают по ходу партии. Место
для него уже есть — меню паузы показывает те же счётчики («как идёт партия»), и это
сторожит проба `pause-menu-everywhere`. Шапка — для того, за чем следят.

### Причина Б — служебный ряд стоит НАД полем (11 экранов)

| экран | верх поля | лишнее | низ объявлен |
|---|---|---|---|
| `anagrams` · `chinese-tones` · `dictation` · `phoneme-pairs` | 173 | +54 | `answer` |
| `breathing` · `cpt` · `mahjong` · `pattern` · `words-hub` | 173 | +54 | не объявлен |
| `mental-rotation` · `spatial-lab` | 175 | +56 | не объявлен |
| `dots-connect` | 227 | +108 | не объявлен |
| `sudoku` | 243 | +107 | — |

⚠️ **ИСТОРИЯ, 11.09.2026.** Тогда лечили переездом в прибитый низ (`bottom="actions"`) или в
полосу счётчиков. С 17.09.2026 причина Б снята для всех разом: каркас ставит служебный ряд
под полем сам, правки в экранах не нужны.

⚠️ Объявление `bottom` — это ещё и запись в реестре смыслов `src/__tests__/slot-meaning.test.ts`.
Реестр описывает, ЧЕМ там отвечают; соврать ему нельзя, гейт краснеет и называет файл.
11.09.2026 на этом уже полежал `main`.

### Отдельно: −71 у пяти экранов «Поиска» — это НЕ дефект

`schulte`, `visual-search`, `find-differences`, `mahjong`, `object-tracker` держат сдвиг
НАРОЧНО: `reserveBottom` из `src/games/search/layout.ts` резервирует место под полосу
ответа, которой у них нет, чтобы в «Зарядке» поле не прыгало между перемешанными
пробами. Разброс внутри раздела — 0. Трогать без владельца нельзя.

## Как перемерить

```bash
cd frontend
NODE_OPTIONS=--max-old-space-size=8192 npx expo export -p web --output-dir dist-geom
node scripts/screen-geometry.mjs dist-geom
```

Прибор печатает отклонения и раскладывает лишнее на «полоса» и «ряд над полем», так что
владельцу экрана не нужно догадываться, какой из двух рычагов его.

⚠️ Мерить **статическую сборку**, а не Metro: 99 маршрутов против Metro кладут процесс.
И показ обязан снимать префикс `/psygames-web`, иначе страница отрисуется, а JS не
загрузится — экран молча покажет режим по умолчанию, и замер будет не про ту игру.
