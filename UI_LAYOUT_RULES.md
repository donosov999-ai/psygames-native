# Расположение экрана игры — закон

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная · 12.09.2026 · обновлён 17.09.2026*

> **Чем это отличается от `SPEC_SCREEN_GEOMETRY.md`.** Тот файл — ОТЧЁТ ЗАМЕРА:
> сколько экранов на каноне, кто отходит и почему. Он устаревает с каждой починкой.
> Здесь — ЗАКОН: где что стоит и по какому правилу. Он меняется только решением.
>
> Числа не назначены, а замерены: собранный веб, окно 390×844, приборы
> `frontend/scripts/screen-geometry.mjs` и `search-chat/geom.mjs`.
>
> 🔴 Единственный источник значений — `frontend/src/components/gameLayout.ts`.
> Этот файл объясняет ПОЧЕМУ; брать числа надо оттуда, а не отсюда копировать.

---

## 1. Порядок сверху вниз

```
  (панели телефона скрыты на всё время экрана игры — правило 11)
┌────────────────────────────────────────┐
│ ШАПКА                          58 pt   │  пауза · заголовок · правый слот (питомец, справка)
├────────────────────────────────────────┤
│ ПОЛОСА ПОКАЗАТЕЛЕЙ             61 pt   │  ходы · время · ошибки
├────────────────────────────────────────┤  ← ВЕРХ ПОЛЯ = 119
│ строка задания над доской (у кого есть)│  ПОСТОЯННОЙ высоты: меняется текст, не высота
│              ИГРОВОЕ ПОЛЕ              │  центр — в СВОЁМ остатке; сторона — от места в поле (правило 5)
│ органы ответа внутри поля (у кого есть)│  цифры, крестовина — сразу под доской
├────────────────────────────────────────┤
│ РЯД СЛУЖЕБНЫХ ЗНАЧКОВ          61 pt   │  прибит сразу под полем: отменить · заново · подсказка · режимы (правило 1)
├────────────────────────────────────────┤
│ ПОЛОСА ОТВЕТА                 120 pt   │  только у игр с ответом кнопками внизу; поля по 66 с боков
└────────────────────────────────────────┘
 ◉ «Сообщить о проблеме» плавает поверх слева снизу — не занимает место, но закрывает (правило 12)
```

| величина | pt | что это |
|---|---|---|
| `ШАПКА` | 58 | шапка каркаса |
| `ПОЛОСА_ПОКАЗАТЕЛЕЙ` | 61 | счётчики под шапкой |
| `ВЕРХ_ПОЛЯ` | **119** | сумма двух верхних |
| `РЯД_ДЕЙСТВИЙ` | 56 | «Отменить», «Подсказка», «Заново» |
| `ВЫСОТА_РЯДА_СЛУЖЕБНЫХ` | **61** | ряд значков под полем: 8 + 48 + PAD_V 5 (`GameShell`, с 17.09.2026) |
| `ПОЛОСА_ОТВЕТА` | 120 | низ с кнопками ответа |
| `ПОЛЯ_ОТВЕТА` | 132 | 66 слева + 66 справа |
| `ПАЛЕЦ` | **48** | минимум для попадания пальцем |
| `ДОПУСК` | 40 | разрешённое отклонение от центра |

---

## 2. Двенадцать правил

### Правило 1. Низ принадлежит ОТВЕТУ, служебное — рядом значков ПОД ПОЛЕМ

> Низ (`toolbar`) — ответ игрока. Служебное (подсказка, «Открыть пару», «Показать решение»,
> отмена, «Начать заново», перемешать, повтор звука, «СТОП») — одним рядом compact-значков
> сразу под полем. Шапка и полоса счётчиков служебного не несут.

🔴 **Решение Дениса 17.09.2026** по кадру «Соедини точки», где подсказки стояли подписанными
пилюлями двумя рядами над полем: «их место снизу иконками под окном упражнения, рядом с
„Отменить“ и „Начать заново“… это надо везде такое правило делать». То же говорит приёмка
16.09.2026 (CHATS_RULES §4б п.4): «вниз под полем — элементы управления».

**Как.** Игра отдаёт служебное каркасу — `headerActions={<GameAuxBar>…<GameAuxAction …/></GameAuxBar>}`
и `solution={…}` для «Показать решение». Каркас ставит всё одним рядом значков под полем
(`game-aux-row`). Внутри ряда кнопка сама становится значком: слово уходит в подпись для чтеца,
число ресурса остаётся. Свой ряд под доской (головоломки, «Точки», «Одна линия», лаборатория)
допустим только тем же видом: `GameAuxAction compact`, одной строкой.

**История (почему не шапка).** С 19.08 до 17.09.2026 правило звучало «служебное — в шапку»,
и у одного действия было три места: ряд над полем, полоса счётчиков (`auxInHud`), прибитый
низ (`bottom="actions"`). Ряд над полем отнимал у доски 54–130 pt. Сохранено из того правила
главное: в полосе ОТВЕТА служебному нельзя.

**Откуда.** Аудит 43 игр 19.08.2026: в 17 играх внизу ответ, и человек,
натренированный «Фланкером», бьёт туда же в маджонге — а там «Перемешать»,
которого три на уровень. Нажал не думая — потерял ход.

🔴 **Игра ОБЪЯВЛЯЕТ низ явно**: `bottom="answer"` (ответ кнопками внизу) или
`bottom="actions"` (ответа кнопками нет). С 17.09.2026 объявление место служебного не меняет.

⚠️ **Служебное правится ВМЕСТЕ с реестром** `AUX_UNDER_FIELD` в `slot-meaning.test.ts` —
одним коммитом. Разведёшь — гейт `slot` остановит выпуск, и это уже стоило одного круга
сборки 12.09.2026. Значки объявляются ПРЯМО в ряду (`headerActions={<GameAuxBar>…}` или
ссылкой на одну памятку): гейт разворачивает ссылку на один шаг и считает действия.

**Что уточнено 17.09.2026 (всё — замерами на собранном вебе):**

1. **Переключатель режима — тоже значок, а не подписанная капсула.** `GameAuxAction active`:
   включённый режим — залитая кнопка и белый значок, экран подаёт залитый вариант значка
   (`pencil` против `pencil-outline`). Разница формой, а не оттенком (отчёт 33f21fc6: включённые
   пометки «сливались с экраном», промах стоил жизни). Состояние `selected` у доступности — ТОЛЬКО
   у переключателя: проба лаборатории узнаёт вкладки по нему и приняла каждый значок ряда за
   вкладку (8 вместо 4) — красный CI кандидата 2.54.22.
2. **«Заново» ставит каркас сам, сразу после значка `arrow-undo`,** из пункта паузы `restart`.
   `auxRestart={false}` — только если «Заново» уже стоит в своём ряду экрана (головоломки).
3. **Подсказка и отмена в ряду объявляют замок лестницы** — `ladder="hint"` / `ladder="undo"`
   (гейт `feature-ladder-coverage`). Запертый значок не прячется и не молчит: замок на значке,
   тост «откроется на уровне N» по нажатию, условие — в подписи для чтеца.
4. **Ряд — одной строкой на 360.** Цена переноса замерена: «Галактики», пять команд с подписанным
   переключателем «Пометка» — две строки на 360×640, 360×780 и 375×667, доска −20, кнопка отзыва
   на ряду 673 px² вместо 224. Переключатель значком — одна строка, доска как была.
5. **У режима с крестовиной значки «про решение» — в ПУСТЫХ клетках крестовины:** лампочка справа
   от «↑», «Где ошибка?» слева. Четвёртым в ряду под полем значок сдвигал «Отменить» под кнопку
   отзыва: «Мосты», 360×640 — 996 px²; в клетке крестовины — 6 px², как было.
6. **Капсулы двумя рядами под полем — прямой путь к «цифры закрыты».** Судоку 2.54.20 (отчёт 57a0e9cd,
   Android 384×784): «Подсказка · Отменить · Пометки · Цвет» двумя рядами + «Заново» своей строкой =
   170 pt под полем, второй ряд цифр ушёл под них. Значками — 61 pt (правило 9, п. «бюджет»).

### Правило 2. Полоса показателей занимает место ВСЕГДА, даже пустая

**Откуда.** Замер 11.09.2026: 61 у большинства и 13 у «Тортов» с «Пиццей», где
счётчиков мало и полоса схлопывалась. Их поле вставало на 48 точек выше соседних.
В «Зарядке» пробы идут вперемешку — и это видно как прыжок поля между упражнениями.

Цена резерва — 48 точек пустоты на двух экранах из восьмидесяти. Оно того стоит.

### Правило 3. Вертикаль — правило, а не число

```
центрПоля(высотаОкна, снизу) = 119 + (высотаОкна − 119 − снизу) / 2
отклонение < ДОПУСК (40)
```

**Почему не фиксированное число.** Чат «Поиск» намерил медиану 438 и честно
написал: «это то, что есть, а не то, что решено». Медиана меняется вместе с
составом экранов и не переносится на другой размер окна. Центр, посчитанный из
высот, верен на любом экране.

⚠️ Ужимать `ДОПУСК` можно только вместе с замером: сперва показать, что экраны
держатся, потом опускать число. Иначе гейт краснеет на живых экранах.

🔴 **ЗАМЕР, ПОКАЗЫВАЮЩИЙ ЦЕНУ НАРУШЕНИЯ (аудит 40 головоломок, 12.09.2026).** На
одном и том же окне 390×844 контейнер поля одного размера — 358×358 — стоял верхом
`Y = 335` в «Мостах» и `Y = 213` в «Судоку/Неравенствах». **Разница центра 122 точки.**
Это сравнение ОДИНАКОВЫХ контейнеров, а не разных пропорций рисунка.

Причина ровно та, от которой это правило и защищает: одно выражение центрировало сразу
и поле, и разное число внутренних кнопок, крестовина в части режимов вынесена в нижнюю
полосу, а цифровая клавиатура — внутрь поля. При переключении упражнений в зарядке тело
экрана переезжает вместе с управлением, и человек каждый раз заново ищет глазами поле.

⚠️ Лечится разделением на области «задание / сцена / управление» с общим якорем сцены —
а НЕ `overflow: hidden` и не запретом прокрутки: это спрячет разъезд, оставив содержимое
недостижимым на коротком экране (правило 9).
Источник: `/Users/denisonosov/dev/psygames-game-lab/audit/puzzles-40-2026-09-12/FINDINGS.md`,
пункт P2-08, снимки `layout-bridges-390x844.png` и `layout-solo-390x844.png`.

### Правило 4. Резерв снизу — это «тюнинг на месте»

Экран без полосы ответа резервирует её высоту: `резервПодПолосуОтвета(false) = 120`.
Тогда поле стоит там же, где у соседа с полосой, и в «Зарядке» не прыгает.

Замер: у «Поиска» разброс центра внутри раздела стал **0 вместо 118**.

### Правило 5. Поле вписывается по ОБЕИМ сторонам, и клетка не мельче пальца

*Добавлено 12.09.2026 по замеру `psygames-codex-mac` и починке «Точек».*

Квадратное поле, заданное как `width: '100%'` + `aspectRatio: 1`, считает себя
только по ШИРИНЕ. Высоту никто не спрашивает — и на коротком экране низ уходит
за край. Замер: окно 440×720, доска 424 → нижние **48 px за экраном**.

```
сторона = min(доступная ширина, доступная высота, потолок)
пол     = размер поля × ПАЛЕЦ        // ниже не сжимать
```

🔴 **Пол обязателен.** Без него на 320×640 доска ужалась до 120 px — клетка 30 при
каноне 48. Это обмен «часть поля за краем» на «в клетку нельзя попасть», то есть
в худшую сторону. Замер после пола: 440×720 → клетка 74 · 390×844 → 92 ·
320×640 → 48. За краем ноль на всех трёх.

Не влезает даже по полу — честно вернуть прокрутку страницы: пусть нижние ряды
достаются прокруткой, чем клетка станет меньше пальца.

⚠️ На больших сетках уменьшать клетку бесконечно нельзя. Там нужен явный режим
масштаба с раздельным управлением перемещением и рисованием, а не скрытая
прокрутка тем же жестом.

### Правило 6. Поле, по которому рисуют пальцем, не отдаёт касание браузеру

*Добавлено 12.09.2026.*

Приложение — веб внутри WebView. Пока страница длиннее окна, браузер считает
протаскивание пальцем прокруткой: он двигает страницу, а игра ведёт свой жест.
Два толкования одного касания — отсюда «поле ездит» и «лагает».

Замер A/B в одной сборке: без запрета свайп сдвигал поле на **120–127 px**,
с запретом — **0**.

```
БЕЗ_ЖЕСТА_ПРОКРУТКИ = { touchAction: 'none', overscrollBehavior: 'contain' }
```

🔴 **Вешать на САМ узел жеста, не на контейнер.** `touch-action` действует на
касания, НАЧАВШИЕСЯ на узле. На доске — рисование перестаёт быть прокруткой, а
прокрутка остального экрана цела. На внешнем контейнере — недостижимым станет всё,
что не влезло в окно.

⚠️ Каркас вешает такой запрет на СВОЁ поле, но игра, рисующая внутри СВОЕГО
`ScrollView`, эту защиту обходит: её скроллер стоит между пальцем и защищённым
узлом. Замер 12.09.2026: так устроены девять экранов, защищён был один.

### Правило 7. Стрелки собираются как на клавиатуре

*Решение Дениса 12.09.2026.*

Четыре стрелки управления ставятся **крестовиной**, а не в строку:

```
        ┌───┐
        │ ↑ │          вверх и вниз — одним столбцом, слитно
    ┌───┼───┼───┐
    │ ← │ ↓ │ → │      влево и вправо — по бокам от нижней
    └───┴───┴───┘
```

Вверх и вниз стоят друг под другом одной осью; влево и вправо отходят от нижней
в стороны. Это раскладка стрелок физической клавиатуры и крестовины геймпада.

**Почему так, а не строкой.** Строка `← ↑ ↓ →` ставит противоположные направления
рядом: «вверх» и «вниз» оказываются соседями, а «влево» и «вправо» — по краям через
две кнопки. Рука ищет направление глазами каждый раз, потому что расположение кнопок
не совпадает с направлением, которое они означают. В крестовине совпадает: жмёшь
вверх — палец идёт вверх.

⚠️ **Размер каждой кнопки — не меньше `ПАЛЕЦ` (48).** Крестовина занимает больше
места, чем строка: три ряда вместо одного. Место берётся из отступов, а не из
размера кнопок.

✅ **Долг 12.09.2026 закрыт:** `app/games/puzzles.tsx` ставит стрелки общим `ArrowPad` —
«перевёрнутой Т» (↑ над рядом ← ↓ →), гейт `arrow-pad-is-an-inverted-t`. Пустые клетки
по бокам от «↑» — законное место для значков «про решение» этого режима (правило 1, п. 5).

### Правило 8. Укладка кнопок — по их числу, а стрелок и цифр — по смыслу

*Решение Дениса 12.09.2026.*

| сколько и чего | как укладывать |
|---|---|
| **две** кнопки | в строку: слева и справа |
| **четыре** обычные | прямоугольником 2×2 |
| **четыре стрелки** | крестовиной (правило 7) — направление важнее числа |
| **цифры** | тремя колонками по ТЕЛЕФОННОЙ схеме |

```
  две          четыре           цифры
┌───┬───┐   ┌────┬────┐      ┌───┬───┬───┐
│ ← │ → │   │ 1  │ 2  │      │ 1 │ 2 │ 3 │
└───┴───┘   ├────┼────┤      ├───┼───┼───┤
            │ 3  │ 4  │      │ 4 │ 5 │ 6 │
            └────┴────┘      ├───┼───┼───┤
                             │ 7 │ 8 │ 9 │
                             ├───┼───┼───┤
                             │   │ 0 │   │
                             └───┴───┴───┘
```

🔴 **ТЕЛЕФОННАЯ СХЕМА, А НЕ КАЛЬКУЛЯТОРНАЯ.** Их путают, и разница видна не сразу:
на телефоне сверху `1 2 3`, на калькуляторе и цифровом блоке клавиатуры сверху
`7 8 9`. Берём телефонную: приложение живёт в телефоне, и рука там уже обучена.

**Почему укладка вообще правило, а не вкус.** Переносящаяся строка (`flexWrap`)
раскладывает кнопки по-разному на разной ширине: на одном экране 5 в ряд, на другом
4 и 1, на третьем 3 и 2. Человек каждый раз ищет нужную заново, потому что она
каждый раз в другом месте. Фиксированная сетка стоит одинаково везде.

⚠️ **Размер кнопки — не меньше `ПАЛЕЦ` (48), и он НЕ платит за укладку.** Сетка выше
строки, и место под неё берётся из отступов и из поля, а не из размера кнопок.
Не влезает — правило 5: поле уступает, кнопка не мельчает.

🔴 **ГДЕ ТЕЛЕФОННАЯ СЕТКА НЕ ПРИМЕНЯЕТСЯ — решение Дениса 12.09.2026, дословно:
«вывод цифр в телефонной раскладке не уместен, так как интерфейс порушит».**

Три колонки — это ТРИ РЯДА в высоту. Там, где цифры стоят полосой ответа под игровым
полем, лишний ряд платится высотой поля или уезжает за нижний край: ровно то, что
случилось в судоку (правило 9, замер 360×640). Поэтому:

| где цифры | укладка |
|---|---|
| отдельный ввод числа (код, счёт, ответ полем) | телефонная сетка 3 колонки |
| **полоса ответа под игровым полем** | **строки по 5: `1 2 3 4 5` / `6 7 8 9`** |

**Пять в строке, а не шесть.** `5 × 48 + 4 × 6 = 264` — влезает даже в 320. Раскладка
6+3 (то, что даёт `flexWrap` сейчас) плоха не шириной, а тем, что ряды разной длины
читаются как разные ряды, и девятка с шестёркой каждый раз оказываются в новом месте.
Две ровные строки 5 и 4 стоят одинаково на любой ширине.

⚠️ Число в строке — 5, а не «сколько влезет». Строка задаётся ЯВНО, переносом её
задавать нельзя: перенос и есть та беда, ради которой это правило написано.

✅ **Судоку закрыт 12–17.09.2026:** цифры идут ЯВНЫМИ строками по 5 в портрете (`ПОТОЛОК_В_СТРОКЕ`),
стирание — десятой клавишей второй строки. Ниже — исходная запись долга, для истории.

🔴 **ДОЛГ НА 12.09.2026, ЗАМЕРЕН ДВАЖДЫ:**
· `app/games/sudoku.tsx:2573` — цифры лежат переносящейся строкой:
  `numPad: { flexDirection: 'row', flexWrap: 'wrap' }`. В соседнем комментарии уже
  записана цена: «на 6×6 семь клавиш не влезали в ряд телефона и переносились
  вразнобой» — то есть беда известна и лечилась уменьшением кнопки с 64 до 50,
  а не укладкой. ⚠️ Здесь особый случай: у судоку N бывает 4, 6 и 9, и телефонная
  сетка 3×3 подходит только девятке. Для 4 и 6 форму согласовать отдельно, а не
  растягивать три колонки силой.
· `app/games/puzzles.tsx:498` — стрелки строкой вместо крестовины (правило 7).
  🔴 **И этого мало — поправка по аудиту 12.09.2026 (P2-07).** У «Раскраски карты»,
  «Колышков», «Указателей» и «Распутай» четыре стрелки ЕСТЬ, а кнопки выбора и
  подтверждения нет; авторские движки ждут `CURSOR_SELECT`/`SELECT2` для захвата,
  подтверждения и отмены (`map.c:2513`, `pegs.c:982`, `signpost.c:1528`,
  `untangle.c:1549`). У «Инерции» авторский ввод — ВОСЕМЬ направлений
  (`inertia.c:1605`), а крестовина даёт четыре. То есть крестовина — форма для
  НАПРАВЛЕНИЙ, и она не заменяет команды механики: нужен список требуемых действий
  на семейство игр (4 или 8 направлений · выбрать · положить · отменить · второе
  действие), а не одни и те же четыре кнопки всем.
  ⚠️ Оговорка автора аудита, и она честная: это неполнота ВИДИМОГО управления
  стрелками, а не доказательство, что касанием играть нельзя.

### Правило 9. Ответ, до которого надо доскроллить, — это не ответ

Правило 1 говорит: низ принадлежит ответу. Правило 9 закрывает лазейку в нём:
**органы ответа обязаны быть ВИДНЫ без прокрутки на самом коротком экране**, а не
просто «достижимы».

**Чем померено (12.09.2026, судоку 9×9, стенд 360×640, свежая загрузка):**

| что | замер |
|---|---|
| ряд цифр 1–6 | y 569…619 — видно |
| ряд цифр 7-8-9 | y **625…675** при экране 640 → **35 px за краем**, видно 15 px из 50 |
| область с полем | `clientHeight` 397, `scrollHeight` 516 — прокрутка есть, 119 px |
| клетка доски | 40 px при бюджете, рассчитанном на 28 |

То есть формально всё «достижимо»: подкрути — и цифры появятся. Практически в судоку
цифру жмут после каждой клетки, и человек получил игру, где между «выбрал клетку» и
«поставил цифру» встала прокрутка. Три отчёта за три дня об одном и том же: «нижние
кнопки уплыли с экрана».

🔴 **КАК СЮДА ПРИШЛИ — ЭТО ВАЖНЕЕ САМОГО ДЕФЕКТА.** Клавиатура цифр РАНЬШЕ СТОЯЛА в
прибитом низу. Её оттуда убрали осознанно и записали причину в коде: между доской и
цифрами зияла пустота почти в пол-экрана, рука тянулась вниз через весь телефон.
Причина была настоящая, замер был настоящий, починка была настоящая — **и создала
дефект хуже исходного**, потому что чинили ОДИН размер экрана.

Отсюда рабочее требование:

> **Правку расположения проверять на самом коротком экране из поддерживаемых, а не на
> том, где увидел дефект.** Пустота видна на высоком экране, обрезка — на низком.
> Починишь по одному — сломаешь другой, и узнаешь об этом из отчётов.

**Что делать вместо выбора между пустотой и обрезкой.** Выбирать не надо: сжимается
ПОЛЕ, а не органы ответа. Бюджет высоты считается от того, что реально стоит на
экране (шапка + полоса показателей + ряд действий + ПОЛНАЯ высота клавиатуры с учётом
переносов), остаток отдаётся полю, и клетка не мельче пальца (правило 5). Если после
этого не сходится — прокрутку получает ПОЛЕ, а не ответ.

⚠️ **Число в бюджете обязано совпадать с тем, что рисуется.** В судоку на 12.09.2026
бюджет считался как `height − 330 − 51`, а настоящая высота низа с двумя рядами цифр
(9×50 + 8×6 = 498 точек не влезают в строку и переносятся 6+3) больше. Клетка вышла
40 при бюджете на 28. Это тот же класс, что «мерить нарисованное, а не контейнер»:
константа, написанная под один случай, молча врёт во всех остальных.

✅ **17.09.2026 — бюджет от места, которое реально дал каркас, а не от окна.** Резерв окна
(`height − 330 − 51`) снова соврал, и опять в сторону «цифры за краем»: с 2.54.20 каркас ставит
служебный ряд ПОД полем, а резерв о нём не знал. Замер экспорта, судоку 9×9 уровень 62: 384×784 —
поле 478, низ клавиатуры на 44 pt ниже поля; 360×640 — тоже 44. Починка: экран получает высоту поля
(`useGameFieldHeight` внутри поля или `FieldHeightUp` для экрана над каркасом) и считает сторону
доски от неё: `место = поле − строка задания − зазор − клавиатура целиком − рамка`
(`клеткаПортрета` в `sudoku.tsx`, проба `sudoku-digits-fit-field`). Стало: 384×784 — доска 346 (упор
в ширину), цифры внутри поля с запасом 65; 360×640 — доска 256 вместо 202. Резерв окна остался только
запасным путём до первого замера поля.

> **Любое число «сколько занято вокруг поля» протухает с первой правкой каркаса.** Считать от
> высоты поля, которую каркас отдаёт сам, — тогда правка каркаса экран не ломает.

**Режим, подменяющий органы ответа, встаёт в ТО ЖЕ место той же высоты.** Судоку, режим «Цвет»:
цифры в нём не вводятся (касание клетки красит), и палитра встаёт на место клавиатуры с
`minHeight` клавиатуры — доска при переключении режима не прыгает. Замер: палитра 552–658 в поле
до 723 (384×784), 462–568 в поле до 579 (360×640).

📌 Правило 8 (цифры телефонной сеткой 3×3) и это правило решают РАЗНОЕ и могут тянуть
в разные стороны: сетка 3×3 уже по ширине, но ВЫШЕ на один ряд. Кто чинит — считает
оба варианта в точках на 320×640 и берёт тот, при котором и клавиатура видна целиком,
и клетка не мельче пальца. Просто «сделал по правилу 8» здесь недостаточно.

---

### Правило 10. Ширину в строке даёт `flex`, а не `alignSelf`

Правило 5 требует вписывать поле по ОБЕИМ сторонам. Правило 10 — то же требование к
кнопкам, и промах здесь всегда одинаковый: **высоту ставят числом и помнят, а ширина
«получается сама» — и получается неправильно.**

`alignSelf: 'stretch'` тянет по ПОПЕРЕЧНОЙ оси родителя, а не «во всю ширину»:

| родитель | поперечная ось | что тянет `stretch` |
|---|---|---|
| колонка (`column`) | горизонталь | ширину — работает как ждёшь |
| **строка (`row`)** | вертикаль | **высоту; ширина остаётся по содержимому** |

🔴 **Нижний слот каркаса (`styles.toolbar` в `GameShell`) — СТРОКА.** Значит ряд
ответа, положенный туда, обязан иметь `flex: 1`. Без него ряд сжимается по
содержимому, а внутренний `flex: 1` делит уже скукоженную ширину.

**Чем померено (12.09.2026, «Числовой забег», окно 390×844, собранный бандл):**

| что стояло | замер кнопки руления |
|---|---|
| `alignSelf: 'stretch'` | **26×48** — при пороге 48 вдвое уже пальца |
| `flex: 1` | **79×48** |

Ширина 79, а не 120: слева зарезервирована зона под плавающую кнопку отзыва, левый
край первой кнопки 66. Это законно и в порог укладывается.

**Что делать:**
- ряд ответа в нижнем слоте — `flex: 1`, не `alignSelf: 'stretch'`;
- кнопке пальца ставить `minWidth: 48` рядом с `height: 48` — страховка на случай
  узкого слота: ремень и подтяжки дешевле, чем третий отчёт про мелкие кнопки.

**Кто сторожит:** гейт `tap-field` (`frontend/scripts/tap-target-audit.mjs --mode=field`,
порог 48×48, окно 390×844). Он это и поймал — не `tsc`, не линт и не 45 проб ядра:
геометрию видит только браузер.

---

### Правило 11. Панели телефона скрыты, пока открыт экран игры

*Решение Дениса 17.09.2026: «да, делай на все, как дешевле по объёму работ» — после проверки режима
на своём телефоне в «Числовом забеге» («полноэкранный режим хорошо работает, проверил только что»).*

Каркас (`GameShell`, проп `immersive`, по умолчанию да) прячет часы, батарею, навигацию Android и
полоску «домой» на всё время экрана игры — от настройки до итога. Пауза, отзыв поверх игры и
сворачивание приложения возвращают их; «Продолжить» и возврат в приложение прячут снова. Android —
«липкий» режим: свайп от края показывает панели поверх игры. В меню паузы у каждой игры пункт
«Не скрывать панели телефона» — выбор запоминается на устройстве. Меню разделов и главная — с панелями.

**Цена, ради которой это сделано:** Android тестировщицы — окно 384×784 при плотности 2,8125, экран 832:
панели забирали **48 pt**. На ширину-ограниченном экране (судоку на 384) это пустота снизу, на коротком
(360×640) — +45 pt доске (замер 17.09.2026: 202 → 247 без правки кнопок). ⚠️ На iPhone с «островом»
безопасная зона сверху остаётся — выигрыш меньше, не мерен.

⚠️ **Игра сама `useImmersive` не зовёт** — это делает каркас. Два хука спорили бы за одну команду
(поэтому у «Числового забега» своя строка снята). Экран, которому панели нужны: `immersive={false}` —
тогда и пункта в меню паузы у него нет. ⚠️ В каркасе хук стоит ПОСЛЕ подписок меню паузы на режим:
иначе объявление режима звучит в пустоту и пункта нет (поймано пробой `pause-menu-fullscreen-only-where-declared`).

### Правило 12. Кнопка «Сообщить о проблеме» плавает слева снизу — под ней не должно быть центра органа

Кнопка отзыва (`fabPosition.ts`: 48×48, x 14–62, поднята на `FAB_BOTTOM`) места не занимает и
закрывает то, что под ней. Мерить ДВА числа, а не «видно — не видно»: площадь пересечения с органом
(px²) и попадает ли ЦЕНТР органа под кнопку. Центр под кнопкой — дефект (нажатие уходит в отзыв);
угол под кнопкой — терпимо, но число записывается.

**Замеры 17.09.2026:** «Мосты» 360×640 — «Отменить» 996 px² при значке «Где ошибка?» четвёртым в ряду
→ 6 px² в клетке крестовины (правило 1, п. 5). «Галактики» 360×640 — 673 → 160 px² после ряда в одну
строку. Судоку 9×9 360×640 — угол клавиши «6» 570 px², центр свободен (открыто).

Средства по порядку дешевизны: не класть орган в левый нижний угол (клетки крестовины, центрированный
ряд) · `reserveUnderFab` каркаса — запас под кнопкой в прокручиваемом поле · `FAB_CLEARANCE` для текста.

---

## 3. Что канон, а что своё

**Канон** — только то, что обязано совпадать у всех: высоты полос над полем и под
ним и производные от них. Их правят в `gameLayout.ts` и нигде больше.

**Канон-компоненты** (свою копию не рисовать — так уже разъезжались крестовины и списки):
`GameAuxAction` / `GameAuxBar` — служебные значки и переключатели ряда под полем · `ArrowPad` —
стрелки крестовиной · `DropdownSelect` — выпадающий выбор значения на настройке (решение Дениса
17.09.2026: «общий делать») · `GameFieldHeight` / `useGameFieldHeight` / `FieldHeightUp` — высота поля
для доски · `GameShell` `immersive` — полный экран.

**Своё у раздела** — вид, цвета, внутренняя вёрстка, сетка, анимации. Сводить их
значило бы запретить играм отличаться.

**Почему один файл.** Раньше числа выводил каждый раздел сам, и в комментарии
«Поиска» прямо стояло: «свои 118 или 124 развели бы разделы между собой, и скачок
вернулся бы, только реже». Два чата держали константы в согласии ВРУЧНУЮ — это
расходится первым: правку вносят в один модуль, второй остаётся старым, и оба
по-своему зелёные.

---

## 4. Что делает новая игра

1. Берёт числа из `gameLayout.ts`, не выписывает свои.
2. Объявляет `bottom="answer"` или `bottom="actions"`, служебное отдаёт каркасу
   (`headerActions`, `solution`) — и правит реестры `slot-meaning.test.ts` тем же коммитом.
3. Отдаёт счётчики каркасу данными (`hud={[…]}`), а не рисует шапку сама.
4. Передаёт `pauseActions` — меню паузы это слот каркаса, не игры.
5. Если поле квадратное — считает сторону по правилу 5.
6. Если по полю рисуют пальцем — вешает `БЕЗ_ЖЕСТА_ПРОКРУТКИ` по правилу 6.
7. Если есть стрелки управления — ставит их крестовиной по правилу 7.
8. Кнопки и цифры укладывает по правилу 8, а не переносящейся строкой.
9. Переключатели режимов — `GameAuxAction active`; подсказка и отмена — с `ladder` (правило 1).
10. Сторону доски считает от высоты поля каркаса, а не от высоты окна (правило 9).
11. Полный экран не включает сам — его ставит каркас (правило 11).
12. Выбор значения на настройке — `DropdownSelect`, не ряд кнопок и не своя копия списка.
13. Проверяет живьём на трёх размерах окна, а не на одном, — и меряет кнопку отзыва над органами (правило 12).

---

## 5. Кто это сторожит

| гейт | что ловит |
|---|---|
| `frontend/scripts/screen-geometry.mjs` | верх поля и высоту полос по всем экранам |
| гейт `slot` (`slot-meaning.test.ts`) | служебного нет в полосе ответа; заявленное отдано каркасу; объявление низа |
| `scripts/slot-audit.mjs` | живьём: служебное нарисовано рядом под полем, а не над полем и не в полосе ответа |
| `tap-target-audit.mjs` | размер цели меньше `ПАЛЕЦ` |
| `board-keeps-its-touch.test.ts` | поле с жестом без запрета прокрутки |
| `feature-ladder-coverage.test.ts` | подсказка и отмена без ключа лестницы |
| `sudoku-digits-fit-field.test.ts` | колонка «строка + доска + клавиатура» выше поля при любом поле 300–900 |
| `pause-menu-fullscreen-only-where-declared.test.tsx` | полный экран: пункт паузы у любой игры на каркасе и нет у `immersive={false}` |
| `scratchpad/sim/aux-row-sweep.mjs` (координатор) | живьём по всем экранам: служебное над полем, ряд в 2+ строки, за краем, под кнопкой отзыва |
| `hub-not-empty-in-every-profile` | пустое поле у какого-то профиля |

⚠️ Зелёный гейт — не приёмка. Приёмка — **открыть и сыграть** на собранном вебе, а
для жестов и прокрутки — на устройстве: настольный Chromium не имеет резинки
WebKit, а Safari на столе не равен установленному WKWebView.

---

## 6. Кто держит его актуальным

**Координатор `psygames-claude-mac`.** Не «все понемногу» — тогда не будет никто.

🔴 **Новое правило вписывается В ТОТ ЖЕ ЗАХОД, когда принято**, вместе с тем, ЧЕМ
померено и что стоило его отсутствие. Решение, принятое и не записанное, через
неделю выводится заново — и часто иначе.

**Ссылка на этот файл стоит в пяти местах:** `CLAUDE.md` репозитория (грузится
автоматически при старте сессии), `CHATS_RULES.md`, одиннадцать `SPEC_CHAT_*.md`,
двенадцать журналов разделов и реф координатора. Это УКАЗАТЕЛИ, а не пересказ:
правило меняется здесь, копии трогать не надо. Пересказ — то самое, что разъехалось
у `layout.ts` двух разделов и ради чего этот файл появился.

⚠️ Устаревший стандарт хуже отсутствующего: по отсутствующему спросят, по
устаревшему сделают молча и неправильно.

---

## 7. Что этот файл НЕ решает

Он не описывает вид: цвета, скругления, тени, шрифты. Не описывает раскладку
внутри поля — это дело раздела. И не заменяет замер: любое новое число сюда
попадает только вместе с тем, чем и на чём оно померено.
