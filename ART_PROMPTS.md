# Промпты отрисовки — Синапс и пробирки

**Автор:** Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная
Заведено 05.09.2026. Держать рядом с ассетами: перерисовка без этих ограничений ломает нарезку.

---

## §0. ТРИ ОГРАНИЧЕНИЯ, БЕЗ КОТОРЫХ ЛИСТ НЕПРИГОДЕН

Это не пожелания к красоте, а требования конвейера. Лист, нарушивший любое из
трёх, приходится перерисовывать целиком.

1. **Фон строго однородный, и ключ у каждого конвейера СВОЙ.** Прозрачности
   генератор не отдаёт, фон выбивается хромакеем.

   🔴 **Ключей два, и перепутать их — значит получить лист, который не режется.**
   Замер 05.09.2026 по пикселю в углу листов:

   | конвейер | ключ | что им резалось |
   |---|---|---|
   | `psygames-astro/_sync/synapse-v2/` | **зелёный `#07f80d`** (просили `#00FF00`) | базовые 20 кадров кота, робота, Нейрона |
   | `mascot-engine-psygames-actions/` | **оранжевый `#ff7a1a`** | доснятые `eat` и `celebrate` |

   Первая редакция этого файла называла оранжевый единственным — неверно.
   Обновляешь мастер-лист Синапса — фон ЗЕЛЁНЫЙ, иначе `build_synapse_spritepack.py`
   его не выбьет. Правило общее: ключа не должно быть в самом персонаже (у кота
   свечение бирюзовое, а не зелёное — потому зелёный и годится).

2. **Сетка равных ячеек, персонаж одного размера и на одной линии во всех.**
   Нарезчик режет лист арифметикой: `--cols N --rows M --size 512`. Он не ищет
   персонажа. Уехал на полклетки — уехал в игре, и цикл дёргается.
   Поэтому в промпте всегда: «одинаковый размер во всех ячейках», «ступни на
   общей нижней линии», «поля вокруг не менее 12% ширины ячейки».

3. **Ни текста, ни цифр, ни рамок, ни подписей, ни нумерации ячеек.** Всё это
   попадает в кадр и остаётся в игре.

📍 Замер 05.09.2026: первый лист пробирок сгенерился с тонкими разделительными
линиями между ячейками — их не просили, но и не запретили. Линии пережили ключ и
попали в вырезанный кадр как артефакт у края. Лечится резом с отступом 4% от
границ ячейки, но правильнее — запретить в промпте.

---

## §1. СИНАПС — лист состояний

**Что сейчас:** 7 состояний × 4 кадра, 512×512 webp с прозрачным фоном:
`walk · idle · wave · jump · sleep · eat · celebrate`.
Лежат в `frontend/assets/images/pet/cat/<состояние><0..3>.webp`.

**Сетка:** 4 столбца × 7 рядов = 28 ячеек, соотношение листа 4:7.
Ряд = одно состояние, слева направо — фазы цикла.

⚠️ **Якоря.** Поверх кадров садятся аксессуары из магазина (шляпа, бабочка,
очки) по трём точкам: `head_top`, `eyes`, `neck`. Значит во ВСЕХ кадрах голова
должна быть видна целиком и не заслонена лапой — иначе вещь съедет. Жалоба Вали
19.08.2026 «бабочка то на пузе, то на хвосте» — это ровно про уехавшие якоря.

```
Сетка 4×7 (четыре столбца, семь рядов, ровно 28 равных ячеек) на СПЛОШНОМ
однородном фоне #FF7A1A. Разделительных линий между ячейками НЕТ.

Персонаж: «Синапс» — нейро-кот, дружелюбный, сиренево-лавандовый мех, крупные
выразительные глаза, короткие лапы, пушистый хвост, мягкий округлый силуэт.
Стиль: объёмная мультипликационная отрисовка — читаемый объём, материал меха,
мягкий верхний свет и контактная тень, БЕЗ плоской заливки и БЕЗ контурной
обводки. Один и тот же персонаж во всех 28 ячейках: та же морда, тот же оттенок
меха, та же длина хвоста.

Вид: строго сбоку-три четверти, поворот одинаковый во всех ячейках.
Размер персонажа ОДИНАКОВЫЙ во всех ячейках, ступни на общей нижней линии,
поля вокруг не менее 12% ширины ячейки. Голова видна целиком и ничем не
заслонена ни в одном кадре.

Ряды — состояния, слева направо четыре фазы одного цикла:
1) IDLE — стоит и дышит: 1 покой, 2 грудь чуть вверх, 3 пик вдоха, 4 выдох
2) WALK — идёт вправо: 1 контакт, 2 перенос, 3 контакт другой лапой, 4 перенос
3) WAVE — машет передней лапой: 1 лапа внизу, 2 поднята, 3 в верхней точке, 4 назад
4) JUMP — прыжок: 1 присед, 2 отрыв, 3 верхняя точка, 4 приземление
5) SLEEP — спит клубком: 1 ложится, 2 свернулся, 3 глубокий сон, 4 хвост дрогнул
6) EAT — ест: 1 наклон к еде, 2 захват, 3 жуёт, 4 облизывается
7) CELEBRATE — радуется победе: 1 подобрался, 2 лапы вверх, 3 пик радости, 4 назад

Фазы одного ряда — ОДИН цикл: между 4-й и 1-й переход должен быть незаметен.

Без текста, без цифр, без подписей, без нумерации, без рамок, без теней на фоне,
фон строго однородный #FF7A1A во всех 28 ячейках.
```

**Нарезка после генерации:**
```bash
python3 tools/cut_animcycle.py --cols 4 --rows 7 --size 512 --chroma ff7a1a <файл>
```

---

## §1а. СИНАПС — расширенный лист 7×9 (63 кадра)

Просьба Дениса 05.09.2026: тот же лист, но 7 столбцов на 9 рядов, «буду пробовать
более красиво». Против нынешних 4×5 это вдвое длиннее цикл (7 фаз вместо 4) и на
четыре состояния больше.

**Сетка:** 7 столбцов × 9 рядов = 63 ячейки. Соотношение листа — **4:5**: при нём
ячейка выходит 36/35, то есть практически квадратной. 7:9 генератор не принимает,
а 3:4 даёт заметно вытянутую ячейку.

**Ряды:** idle · walk · wave · jump · sleep · eat · celebrate · think · sad.
Первые семь — то, что код уже умеет (`PetState` в `PetSprite.tsx`). Два последних
новые: «думает» на время партии и «расстроен» на провал уровня.

⚠️ **63 ячейки за один заход рискованнее двадцати.** Даже на листе 4×5 ряд прыжка
пришлось переснимать отдельно — рядом лежит `jump-repair-3x3-4k.png`. Чем больше
ячеек, тем сильнее модель уводит внешность к концу листа. Запасной путь, если
поплывёт: генерировать по одному ряду полосой 7×1, подавая нынешний мастер как
IMAGE 1, — тогда персонаж держится за образец, а не за соседнюю ячейку.

⚠️ **Код ждёт четыре кадра на состояние.** Вшитые наборы в `PetSprite.tsx`
перечислены поимённо (`idle0…idle3`). Семь кадров — правка в этом файле; она
механическая, но без неё лишние три кадра просто не будут показаны. У облика из
канала такого ограничения нет: там длина съёмки приезжает полем `frames`.

Промпт целиком — `~/dev/psygames-astro/_sync/synapse-v2/cat/prompt-7x9.txt`.

**Нарезка** (границы считаются от размера листа, ряды равной высоты):
```bash
cd ~/dev/psygames-astro/_sync/synapse-v2/cat
python3 build_synapse_spritepack.py \
  --master master-7x9-4k.png --prompt prompt-7x9.txt \
  --x-bounds ... --y-bounds ... \
  --output ~/dev/psygames/frontend/assets/images/pet/cat
```
⚠️ `--x-bounds` принимает 5 чисел, `--y-bounds` — 6: они зашиты под сетку 4×5.
Под 7×9 сборщик надо править, а не звать флагами.

## §2. ПРОБИРКИ — лист форм

**Что сейчас:** одно стекло `frontend/assets/images/games/water-sort/tube-glass.png`,
192×577, прозрачный фон. Рисуется ПОВЕРХ жидкости.

⚠️ **Три вещи, из-за которых первый лист пришлось переделывать:**

- **Пробирка обязана быть ПУСТОЙ и бесцветной.** Модель охотно рисует «стекло с
  жидкостью» — такое стекло в игру не годится: уровень жидкости у нас
  динамический. Внутри должен просвечивать фон.
- **Стекло не должно брать подтон фона.** На оранжевом фоне блики выходят
  оранжевыми, и над синей жидкостью читаются как грязь. Лечится обесцвечиванием
  после ключа (каждый пиксель в свою яркость), но лучше просить холодные
  нейтральные блики сразу.
- **Внутренние поля снимаются С ФАЙЛА, а не на глаз.** В коде лежат четыре числа
  (`ВНУТРИ_СЛЕВА 0.182`, `ВНУТРИ_СПРАВА 0.818`, `ВНУТРИ_СВЕРХУ 0.10`,
  `ВНУТРИ_СНИЗУ 0.955`). Перерисовали стекло — замерьте заново по альфа-каналу,
  иначе жидкость вылезет за стенки.

```
Сетка 3×3 (девять равных ячеек, разделительных линий НЕТ) на СПЛОШНОМ однородном
фоне #FF7A1A.

В каждой ячейке — ОДНА пустая стеклянная лабораторная пробирка: вид строго
спереди, строго вертикально, по центру ячейки, ОДИНАКОВЫЙ размер во всех девяти
ячейках, донья на общей нижней линии, поля вокруг не менее 12% ширины ячейки.

Пробирка ПУСТАЯ: никакой жидкости, никакого содержимого, внутри просвечивает
фон. Стекло БЕСЦВЕТНОЕ: блики холодные бело-серые, БЕЗ оранжевого и любого
другого подтона.

Форма: ровные вертикальные стенки, дно закруглено буквой U, сверху аккуратный
утолщённый ободок, горловина открыта. Пропорции высокие и узкие: высота примерно
2,8–3,0 ширины.

Стекло читается ТОЛЬКО бликами и краями: узкий вертикальный светлый блик слева,
тонкий отражённый свет справа, чуть более тёмный контур по краю стенок, лёгкое
утолщение стекла у дна, мягкая контактная тень под донышком. Блики достаточно
контрастные, чтобы читаться и поверх светлой, и поверх тёмной заливки.

Девять вариантов отличаются ФОРМОЙ И ОТДЕЛКОЙ, не цветом:
1) классическая тонкостенная, простой ободок
2) толстостенная, массивный ободок, заметное преломление у краёв
3) с широким плоским ободком и прямыми плечами
4) с гранёными вертикальными рёбрами по стенке
5) с двойным ободком
6) чуть конусная книзу
7) с очень тонким ободком и глубоким U-дном
8) минималистичная: только тонкий контур и один блик
9) аптечная: короткая шейка, тело чуть шире

Без текста, без цифр, без подписей, без нумерации, без рамок, без градиента на
фоне — фон строго однородный #FF7A1A во всех девяти ячейках.
```

**Что делать после генерации:**
1. Резать с отступом 4% от границ ячейки (страховка от разделительных линий).
2. Хромакей по `#FF7A1A`, порог ≈30, мягкий край.
3. Обесцветить: каждый пиксель → своя яркость (стекло бесцветно).
4. Обрезать по альфе > 70.
5. Снять четыре внутренних поля по альфа-каналу и вписать в
   `app/games/water-sort.tsx`.
