/* psygames-test-helper-hubs · VER 1 · 04.09.2026 */
/**
 * ИМЕНА РАЗВИЛОК ВЫВОДЯТСЯ ИЗ КАТАЛОГА, А НЕ ВЫПИСЫВАЮТСЯ В КАЖДОМ ГЕЙТЕ.
 *
 * 🔴 ЗАЧЕМ. У проекта уже есть запись об этой грабле: «имена хабов лежали в пяти
 * местах кода и в гейте шестым; третий хаб обязан был попасть в каждое»
 * (game-standard.test.ts). 04.09.2026 развилок стало девять разом — и три гейта
 * (`undo-honesty`, `game-task-line`, `games-registry-covers-all`) покраснели именно
 * потому, что держали список руками.
 *
 * Признак развилки один: `hub: true` в карточке каталога. Его и читаем.
 * Комментарии срезаем — слово «hub» в объяснении не должно сходить за пометку.
 */
declare function require(m: string): any;
declare const __dirname: string;
const fs = require('fs');
const path = require('path');

export function hubScreenFiles(): Set<string> {
  const каталог = path.join(__dirname, '../../constants/games.ts');
  const raw: string = fs.readFileSync(каталог, 'utf8');
  const src = raw.replace(/\/\*[\s\S]*?\*\//g, ' ').replace(/^\s*\/\/.*$/gm, ' ');
  const из = new Set<string>();
  for (const m of src.matchAll(/\n {2}\{\n([\s\S]*?)\n {2}\},/g)) {
    if (!/^\s*hub:\s*true,?\s*$/m.test(m[1]!)) continue;
    const r = /route:\s*'\/games\/([a-z0-9-]+)'/.exec(m[1]!);
    if (r) из.add(`${r[1]}.tsx`);
  }
  if (!из.size) throw new Error('в каталоге не нашлось ни одной развилки — разбор сломался');
  for (const f of РАСФОРМИРОВАННЫЕ) из.add(f);
  return из;
}

/**
 * 🔴 РАСФОРМИРОВАННЫЕ РАЗВИЛКИ: КАРТОЧКИ В КАТАЛОГЕ НЕТ, А ЭКРАН НА ДИСКЕ ЕСТЬ.
 *
 * 12.09.2026 решением Дениса пять развилок расформированы, их игры разошлись по
 * другим разделам (задачи TeamOps 80eb49c9, 4dce5eb9, 81486a4a, 8f0b0428,
 * 43bc1ca2). Карточки `hub: true` из `games.ts` сняты — и четыре гейта разом
 * перестали узнавать эти экраны как развилки, потому что признак берётся из
 * каталога.
 *
 * ⚠️ ФАЙЛЫ ОСТАВЛЕНЫ НАРОЧНО. В каждой из пяти задач стоит прямое указание
 * Дениса: «удалять только по отдельному слову». Слова он не давал, и удалять
 * молча нельзя — у него на это отдельное правило.
 *
 * ЭТО НЕ ДЫРА В ГЕЙТАХ, А РАСПИСКА. Список умеет только сокращаться: проба
 * `список расформированных не протух` ниже краснеет, если файл уже снесли (тогда
 * строку убрать) или если развилку вернули в каталог (тогда тем более). Дал
 * Денис слово на снос — удаляются пять файлов И пять строк отсюда, одним заходом.
 */
export const РАСФОРМИРОВАННЫЕ = new Set<string>([
  'inhibition-hub.tsx',
  'risk-hub.tsx',
  'routes-hub.tsx',
  'flexibility-hub.tsx',
  'visual-memory-hub.tsx',
]);
