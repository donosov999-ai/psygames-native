/**
 * ИСТОЧНИКИ ДАННЫХ И ГРАФИКИ — И ЛИЦЕНЗИИ, КОТОРЫЕ ЭТОГО ТРЕБУЮТ.
 *
 * 🔴 ЗАЧЕМ ОТДЕЛЬНЫЙ ЭКРАН, А НЕ СТРОЧКА В README. Часть лицензий требует
 * указания авторства В САМОМ ПРОДУКТЕ, а не в репозитории. Замер 04.09.2026:
 * шахматные фигуры Cburnett приехали в 2.37.5 под BSD-3, константа с текстом
 * уведомления была заведена — и НИ РАЗУ НЕ ПОКАЗАНА. То есть требование
 * лицензии формально не выполнялось, хотя выглядело выполненным.
 *
 * ⚠️ Лицензия РЕПОЗИТОРИЯ и лицензия ДАННЫХ в нём — разные вещи; бейдж GitHub
 * читает только корневой LICENSE. Каждая запись ниже проверена чтением файла
 * лицензии, а не бейджа. Разбор ловушек — `SOURCES_CATALOG.md` §9.
 */
export interface SourceEntry {
  /** Короткое имя источника. */
  name: string;
  /** Где взято. */
  url: string;
  /** Лицензия — ровно та, что в файле лицензии источника. */
  license: string;
  /** Авторство, если лицензия требует его сохранять (BSD-3, MIT). */
  credit?: string;
  /**
   * Ключ словаря с описанием «что именно взято». Не пара ru/en прямо здесь:
   * зашитые двуязычные строки на экранах верхнего уровня запрещены гейтом
   * `screen-language-fallback` — иначе текст не доедет до остальных десяти языков.
   */
  key: string;
}

export const SOURCES: SourceEntry[] = [
  {
    name: 'HSK 3.0 (ivankra/hsk30)',
    url: 'https://github.com/ivankra/hsk30',
    license: 'MIT',
    credit: 'Ivan Krasilnikov · Shawky · Pleco Inc.',
    key: 'sourceHsk30',
  },
  {
    name: 'Шахматные фигуры Cburnett',
    url: 'https://en.wikipedia.org/wiki/User:Cburnett/GFDL_images/Chess',
    license: 'BSD-3-Clause',
    credit: 'Colin M.L. Burnett',
    key: 'sourceCburnett',
  },
];

/**
 * Лицензии, которые обязывают показывать авторство в приложении. Список
 * используется гейтом: у такой записи `credit` не может быть пустым.
 */
export const CREDIT_REQUIRED = ['BSD-3-Clause', 'MIT', 'Apache-2.0', 'CC BY', 'CC BY-SA'];
