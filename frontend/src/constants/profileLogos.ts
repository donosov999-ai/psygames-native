// Лого-вордмарк «PsyGames» под профиль — 9 вариантов из одной картинки (assets/images/logos/).
// «Пока в каждом режиме свой» — каждый профиль показывает свой стиль надписи на главном экране.
// Прозрачный фон (фон вычищен chroma-key при нарезке). Разные пропорции → показывать в боксе resizeMode=contain.
const LOGOS = [
  require('../../assets/images/logos/logo0.webp'),  // 0 фиолет-баблы
  require('../../assets/images/logos/logo1.webp'),  // 1 неон
  require('../../assets/images/logos/logo2.webp'),  // 2 золото-сериф
  require('../../assets/images/logos/logo3.webp'),  // 3 радуга-конфета
  require('../../assets/images/logos/logo4.webp'),  // 4 тех-градиент
  require('../../assets/images/logos/logo5.webp'),  // 5 гейминг-щит
  require('../../assets/images/logos/logo6.webp'),  // 6 мозг + надпись (бренд)
  require('../../assets/images/logos/logo7.webp'),  // 7 минимал
  require('../../assets/images/logos/logo8.webp'),  // 8 хром
  require('../../assets/images/logos/logo9.webp'),  // 9 teal + лотос-волна (спокойный, 50+)
  require('../../assets/images/logos/logo10.webp'), // 10 глобус-речь мультиколор (языки)
  require('../../assets/images/logos/logo11.webp'), // 11 зелёный + подарок (free, приветливый)
];

// Профиль → индекс варианта. v1.124.0: сгенерены 3 новых лого (9/10/11) →
// теперь у ВСЕХ 12 профилей СВОЙ уникальный вордмарк (индексы 0-11 без повторов).
// Было: seniors=vasilyeva=8, polyglot=students=4, free=odv999=6 делили лого.
const PROFILE_LOGO: Record<string, number> = {
  odv999: 6, nzt48: 1, chess: 2, kids: 3, students: 4, drivers: 5,
  women: 0, vasilyeva: 8, execs: 7, seniors: 9, polyglot: 10, free: 11,
};

export function logoForProfile(id?: string) {
  return LOGOS[PROFILE_LOGO[id ?? ''] ?? 6];
}
export const LOGO_VARIANTS = LOGOS;

/* ─────────────────────────────────────────────────────────────────────────
 * ПЛАШКА ПОД ВОРДМАРКОМ — СВЕТЛАЯ ИЛИ ТЁМНАЯ, ПО САМОМУ ЗНАКУ (VER 1 · 26.08.2026)
 *
 * 🔴 ЖАЛОБА ДЕНИСА, ИЗ КОТОРОЙ ЭТО ВЫРОСЛО: «предприниматели (лого псигеймс)
 * херово видно там, НЗТ 48 тоже». Причина найдена замером, а не на глаз.
 *
 * Плашка под лого бралась цветом ТЕМЫ (`colors.surface + 'CC'`) — то есть на
 * тёмной теме она тёмная ВСЕГДА, независимо от того, какого цвета сам знак.
 * Средняя яркость непрозрачных пикселей знака (0–255):
 *   logo5 61 · logo6 68 · logo7 71 · logo1 77 · logo11 89 · logo4 100 · logo9 119
 *   logo0 125 · logo10 143 · logo3 148 · logo8 150 · logo2 163
 * У `execs` это logo7 = 71: тёмно-серая надпись на тёмно-серой плашке. Ровно то,
 * что Денис и увидел.
 *
 * ⚠️ ПО ОДНОЙ ЯРКОСТИ ПРАВИЛО НЕ СТРОИТСЯ, И Я ЭТО ПРОВЕРИЛ. Напрашивалось
 * «темнее 130 → светлая плашка». На logo7 (71) верно, а на logo1 (77) — НЕТ:
 * это неон, и он несёт СВОЙ тёмный ореол. На светлой плашке ореол становится
 * чёрной кляксой. Поэтому таблица составлена просмотром всех двенадцати знаков
 * на обеих плашках, а не формулой.
 *
 * ПОЧЕМУ ПО УМОЛЧАНИЮ СВЕТЛАЯ. Плашка лежит поверх фото профиля
 * (`profileBackgrounds.ts`), а все девять фонов светлые (яркость 178–241).
 * Тёмный прямоугольник на светлом снимке читается как заплата, светлый — как
 * часть картинки. Тёмная остаётся только там, где знак иначе гибнет.
 */
export type LogoPlate = 'light' | 'dark';
const PLATE_DARK = new Set(['nzt48', 'chess', 'kids', 'vasilyeva']);
//  nzt48      — неон с собственным тёмным ореолом: на светлом плашка даёт кляксу
//  chess      — золотой сериф: на белом истончается и сереет, на тёмном светится
//  kids       — радуга: жёлтые буквы на белом проваливаются
//  vasilyeva  — хром: серое на белом почти исчезает

/** Какая плашка нужна вордмарку этого профиля, чтобы он читался. */
export function logoPlateFor(id?: string): LogoPlate {
  return PLATE_DARK.has(id ?? '') ? 'dark' : 'light';
}
