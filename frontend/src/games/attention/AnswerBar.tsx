/* psygames-attention-answer-bar · VER 1 · 07.09.2026 */
/**
 * ПОЛОСА ОТВЕТА ПОСТОЯННОЙ ВЫСОТЫ.
 *
 * 🔴 ЗАЧЕМ ОБЁРТКА, А НЕ ПРОСТО ОДИНАКОВЫЕ КНОПКИ. Кнопки я уже свёл к трём типам,
 * но высоту полосы задаёт СОДЕРЖИМОЕ: круг 88 даёт полосу 109, две плашки в два
 * ряда — 129, одна кнопка — 93. Замер 07.09 после сведения кнопок: разброс всё ещё
 * 71…129 px. Пока высота считается от содержимого, она будет разной у разных типов
 * ответа, и в «Зарядке» экран продолжит дёргаться между пробами.
 *
 * Здесь высота задана числом (`ANSWER_BAR_H`), а содержимое центрируется внутри.
 * Тогда полоса одинакова у всех десяти независимо от того, что в ней лежит.
 *
 * ⚠️ Ширину НЕ трогаем: её задаёт слот каркаса (`FAB_GUTTER` по 66 с обеих сторон),
 * и игры обязаны считать размеры от неё, а не от своих чисел.
 */
import React from 'react';
import { View } from 'react-native';
import { ANSWER_BAR_ROW } from './layout';

/**
 * ⚠️ 09.09.2026: было `justifyContent: 'center'` — содержимое центрировалось по
 * вертикали, и низ кнопки уезжал вслед за её высотой. Теперь обёртка и семь
 * экранов, державших свою копию стиля, берут ОДИН `ANSWER_BAR_ROW` с якорем
 * снизу. Разбор — в шапке этой константы.
 */
export function AnswerBar({ children }: { children: React.ReactNode }) {
  return <View style={ANSWER_BAR_ROW}>{children}</View>;
}
