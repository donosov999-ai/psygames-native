/* psygames-anagram-hud-report · VER 1 · 07.09.2026 */
/**
 * ОТЧЁТ РЕЖИМА В ШАПКУ — ОДИН ТИП НА ЧЕТЫРЕ РЕЖИМА.
 *
 * 📍 ПРОСЬБА ДЕНИСА 07.09.2026, дословно: «подогнать окно вывода информации по
 * центру и нижние тулбары с кнопками примерно к одной геометрии и виду, пляшет
 * всё». Повод — «Зарядка», которая мешает разные игры подряд: скачущий интерфейс
 * между ними раздражает сильнее, чем плохой в каждой по отдельности.
 *
 * 🔴 ЗАМЕР, ИЗ КОТОРОГО ЭТО ВЫРОСЛО (375×812, мобильная эмуляция):
 *   классика      — панель сверху, y 58, высота 61;
 *   филворды      — панель сверху, y 58, высота 72;
 *   слово-квадрат — панели НЕТ, счётчики на y 809 при экране 812, то есть за краем;
 *   все слова     — панели НЕТ, счётчики под кнопками;
 *   кроссворд     — панели нет, вместо неё строка 322×34 на y 148.
 *
 * ⚠️ ПОЧЕМУ ОТЧЁТ, А НЕ ВЁРСТКА. Каркас `GameShell` принимает `hud` как ДАННЫЕ и
 * сам решает вид — это заказ Дениса от 02.09.2026: «перестраивай каркас так,
 * чтобы потом он легко менялся». Классика так и делает, поэтому панель у неё
 * есть; три остальных режима рисовали счётчики сами, внизу своей колонки, и
 * каркас про них не знал. Здесь режим отдаёт ЧИСЛА, а где и как их показать —
 * забота каркаса, одна на все игры приложения.
 */

export interface ОтчётРежима {
  /** Сколько целей закрыто. */
  найдено: number;
  /** Сколько целей всего. */
  всего: number;
  /** Взятые подсказки — цена уровня. */
  подсказок: number;
  /** Промахи там, где режим их считает (слово-квадрат). */
  промахов?: number;
  /** Бонусные слова копилки — только у «Найди все слова». */
  бонусов?: number;
}
