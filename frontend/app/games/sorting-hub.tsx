/* psygames-game-sorting-hub · VER 1 · 06.09.2026 */
/**
 * Развилка «Сортировка» — разложить предметы по ёмкостям, у которых конечное место.
 *
 * Заведена 06.09.2026 по просьбе Дениса: «нам нужен хаб СОРТИРОВКА (добавим в него
 * эти шарики) и тортики) и что там ещё находили для сортировки)».
 *
 * ⚠️ ШАРИКИ — ЭТО НЕ ОТДЕЛЬНОЕ УПРАЖНЕНИЕ. Приложения «Ball Sort» и «Water Sort»
 * различаются только картинкой: те же ёмкости, то же правило «класть можно на
 * свой цвет или в пустое». Заводить вторую игру ради шариков значило бы получить
 * два входа с одной цифрой роста — поэтому шарики уместны как оформление
 * переливалки, а не как строка развилки.
 *
 * ⚠️ ТОРТИКОВ ЗДЕСЬ ПОКА НЕТ, И ЭТО ЧЕСТНО СКАЗАНО, А НЕ ЗАБЫТО. В «Cake Sort»
 * складывается ШЕСТЬ секторов в круг, а не три в ряд; это меняет арифметику
 * решаемости в «Сортировке товаров», а не добавляет тему. Ложится режимом туда,
 * отдельной игрой — нет.
 *
 * ⚠️ ЧЕМ ОТЛИЧАЕТСЯ ОТ «БАШЕН». Там ход ограничен ПОРЯДКОМ и место не кончается;
 * здесь ограничивает ВМЕСТИМОСТЬ, и главный дефицит — куда положить.
 */
import React from 'react';
import HubScreen from '@/src/components/HubScreen';

export default function SortingHub() {
  return (
    <HubScreen
      hubRoute="/games/sorting-hub"
      titleKey="sortingGroup"
      descKey="sortingGroupDesc"
      pickKey="hubPickExercise"
      footnoteKey="sortingGroupFootnote"
      icon="funnel"
      gradient={['#f7971e', '#0ea5e9']}
    />
  );
}
