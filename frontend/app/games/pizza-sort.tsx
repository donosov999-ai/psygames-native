/* psygames-game-pizza-sort · VER 1 · 07.09.2026 */
/**
 * «Пицца» — тот же движок, что у «Тортов», другая шкурка и своя лестница.
 *
 * Решение Дениса 07.09.2026: «ещё сделать режим пиццы, смысл тот же, картинки
 * разные». Отдельной игрой рядом — ровно так же, как «Шарики» и «Гайки» стоят
 * рядом с «Переливалкой». Разбор и цена решения — в шапке `CakeSortScreen`.
 *
 * ⚠️ ЧЕМ ПИЦЦА ОТЛИЧАЕТСЯ НА ПОЛЕ: круг собирается из шести кусков ровно так же,
 * но вид различается НАЧИНКОЙ, а не цветом глазури. У тортов цвет гарантировала
 * палитра; здесь картинка кроет заливку целиком, поэтому двенадцать начинок
 * подобраны по далеко разнесённым тонам — от красной пепперони до чёрной с
 * чернилами каракатицы.
 *
 * ⚠️ Своя лестница: `gameId` другой, значит и сохранённый уровень, и
 * недоигранная партия у пиццы свои. Пройденное в тортах пиццу не открывает.
 */
import React from 'react';
import { CakeSortScreen } from '@/app/games/cake-sort';

export default function PizzaSortGame() {
  return <CakeSortScreen gameId="pizza_sort" skin="pizza" titleKey="pizzaSort" />;
}
