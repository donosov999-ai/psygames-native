# ТЗ чату «Конфликт внимания» — пять классических проб психометрики

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная · 06.09.2026*

Ты ведёшь один раздел ВДОЛГУЮ. Это самая «научная» часть приложения: все пять
проб — настоящие методики с полувековой историей, и именно на них нас будут
проверять всерьёз.

---

## 1. Твои файлы

```
frontend/app/games/stroop.tsx
frontend/app/games/flanker.tsx
frontend/app/games/cpt.tsx
frontend/app/games/targets.tsx
frontend/app/games/wcst.tsx
frontend/app/games/attention-conflict.tsx   ← развилка
frontend/src/games/attention/**  (если заведёшь)
frontend/src/__tests__/stroop-*  ·  flanker-*  ·  cpt-*  ·  wcst-*  ·  targets-*
```

**За их пределы не выходишь.** Не трогаешь чужие разделы.

**Общий слой — через координатора**, сам не правишь:
`src/constants/games.ts` · `profiles.ts` · `hubContents.ts` · `helpMap.ts` ·
`whatsNew.ts` · `CHANGELOG.md` · версии · метки git.

---

## 2. Правила работы — постоянные

- 🔴 **`git add -A` запрещён.** Только `git add <свои файлы>`. 06.09.2026
  из-за подметания дерева работа трёх сессий уехала в чужие коммиты.
- `git pull --rebase` перед правкой, `git push` сразу после коммита.
- Коммит после КАЖДОГО куска. Подпись `[Claude·MAC]`.
- **Версию не поднимаешь, метку не ставишь, выпуск не собираешь.** Заливка
  приложения целиком — на координаторе: модуль знает 5% приложения и не может
  подписываться под остальными 95%.

**Каждая правка несёт три вещи:** замер ДО числом · гейт, краснеющий на
возврате дефекта · замер ПОСЛЕ. Без замера правка не принимается.

🔴 **Гейт доказывается мутацией.** Верни дефект — проба обязана покраснеть; не
покраснела, значит гейт мерит не то. 06.09.2026 у координатора выжила мутация и
вскрыла ВТОРОЙ дефект, прикрытый первым.

🔴 **Отказ требует такого же замера, что и согласие.** Под словами «нечего
брать», «дефекта нет», «не подходит» обязаны стоять: чем мерил (команда, файл)
· что получилось (число) · что из отклонённого ВСЁ РАВНО переносится.

🔴 **Прежде чем делать — открой и сыграй.** 06.09.2026 пункт «озвучка четырёх
игр» висел месяц несделанным; открыли — сделано полностью. Проверяй состояние,
а не список.

---

## 3. Что уже есть

| экран | методика |
|---|---|
| `stroop` | Струп: назвать цвет, а не слово |
| `flanker` | фланкер Эриксена: центральная стрелка среди отвлекающих |
| `cpt` | продолжительное внимание, 4–12 минут |
| `targets` | поиск мишеней среди помех |
| `wcst` | Висконсинская сортировка карточек: правило меняется молча |

---

## 4. Главная задача: своя лестница КАЖДОМУ режиму

🔴 Задача заведена и висит без исполнителя: **«Конфликт внимания: своя лестница
сложности каждому режиму»**.

Сейчас у этих проб трудность обычно растёт ОДНИМ способом — временем на ответ.
Но пять методик меряют разное, и общая мерка объявит часть режимов пустыми.

⚠️ Ровно на этом обжёгся соседний раздел 06.09.2026: там одна общая мерка
разнообразия объявила три режима из пяти пустыми — не потому что они пустые, а
потому что мерка была не про них. Коммит `Сортировка: у каждого режима своя
мерка`.

Порядок работы:
1. **Сначала мера, потом лестница.** Для каждой из пяти назови величину,
   которую можно померить у КАЖДОГО прохода: у Струпа — разница времени между
   согласованным и конфликтным, у фланкера — цена помехи, у WCST — сколько
   ходов до перехвата нового правила, у CPT — падение точности к концу, у
   мишеней — плотность помех.
2. Лестница строится ПО ЭТОЙ величине, а не по секундам.
3. Гейт прогоняет ВСЕ уровни и требует, чтобы величина не откатывалась.
   Ломается на стыке участков — так находили трижды за неделю.

⚠️ И встречная сторона: у методик есть НОРМЫ. Не делай пробу труднее, чем
методика допускает, — иначе цифра перестанет что-либо значить. Где меняешь
параметр против канона, пиши это в комментарии числом и со ссылкой.

---

## 4а. Что ждёт по отчётам

🔴 **Фланкер: вёрстка едет.** Отчёт NZT-48 04.09.2026: «два косяка — экран
разъезжается, верхний тулбар на две строчки, и стрелки продолжают двигаться;
не зафиксировано, в определённых случаях уходит вбок вниз». Помечено
починенным — **проверь живьём на 360 px**, это класс, который возвращается.

Для замера: шапка обязана влезать в 360 px, гейт `web-gates` в сборке это
проверяет третьим проходом.

---

## 5. Отчёты тестировщиков

Проверяй каждый заход:

```sql
select id, created_at::date, person, game_id, message
from app_feedback
where game_id in ('stroop', 'flanker', 'cpt', 'targets', 'wcst') and status = 'new'
order by created_at desc;
```

Закрывая — пиши `fixed_in_version`, `fix_note` **на языке автора** и
`status='fixed'`. Гейт выпуска это проверяет, и человек увидит ответ.

---

## 6. Чего НЕ делать

- Не переписывать общий каркас экранов (`GameShell`, `LevelCleared`).
- Не заводить второй источник правды рядом с существующим.
- Не поднимать потолок линта. Ратчет обязан сказать «Долг не вырос»; вырос —
  чини СВОИ замечания, а не добавляй их к чужим.
- Не трогать `PROJECT_REF.md` в корне репозитория — он у координатора.

---

## 7. Как отчитываться

TeamOps, канал `psygames`: `journal_add` с ПУТЁМ к файлу на каждый значимый
результат. Задача без пути в итог дня не попадёт.

Готово к выпуску — пиши координатору с числом из `npx jest <твои пробы>`.
Метку ставит он.

---

## 8. Своя папка и свой `PROJECT_REF`

🔴 **Первое, что делаешь — заводишь себе папку.** Файлы раздела не сыплются в
корень: там уже 220+ файлов и найти ничего нельзя.

```
~/Downloads/Code claude/psygames/<ИМЯ>-chat/
    PROJECT_REF.md      ← твой журнал, ведёшь сам
    art/                ← листы картинок, нарезка, контактные листы
```

Соседи уже так живут: `words-chat/`, `sorting-chat/`. Прежде чем создавать —
загляни, нет ли уже твоей папки.

**Что в твоём `PROJECT_REF.md`** (образец — у соседей, форма та же):

- ★ строка на каждый заход: дата, подпись `Claude·MAC`, что сделано, каким
  ЗАМЕРОМ доказано. Числа обязательны: «было 4,2 клетки на путь, стало 6,4», а
  не «стало лучше».
- 🔴 отдельной строкой — каждый найденный дефект и то, чем он был НЕВИДИМ.
- 🚫 отказы: чем мерил, что получилось, что из отклонённого переносится.
- Открытые вопросы к Денису — списком, чтобы он отвечал одним словом.

⚠️ **`PROJECT_REF.md` в корне репозитория — не твой.** Он у координатора и
лежит в `.gitignore` как коммерческая тайна. Твой — в твоей папке.

---

## 9. Картинки: как генерировать и как нарезать

### 9.1. Правила рисования

- 🔴 **Реалистично.** Объём, материал, свет — даже мультяшному предмету. Плоские
  заливки Денис бракует.
- 🔴 **Сетка листа фиксирована: 7 × 8.** Не 4×5, не 3×3 — семь колонок, восемь
  рядов. Спрашивать не нужно, это решено.
- Есть образец — **описывай ПОЗЫ, а не внешность.** Модель и так видит, как
  предмет выглядит; описание внешности поверх образца ломает сходство.
- Геометрию задавай ЧИСЛОМ: «три кольца, зазор в четверть диаметра», а не
  «чтобы не сливались» — иначе модель сливает.
- ⚠️ Модель отказывается рисовать похожих на киногероев. Отказ выглядит
  техническим, причина правовая — переформулируй, не долби.

### 9.2. Чем генерировать

MCP `kie` → `generate_image` (Nano Banana 2). Один лист сеткой — дешевле и
ровнее, чем 56 отдельных вызовов: **одна генерация 7×8, потом нарезка**.

```
mcp__kie__generate_image
  prompt: "<сетка 7×8, ряд = состояние, ровный серый фон, объекты НЕ отбрасывают тень>"
  resolution: "4K"
  output_dir: "~/Downloads/Code claude/psygames/<ИМЯ>-chat/art"
```

🔴 **«Объекты не отбрасывают тень» — обязательный пункт промпта.** Иначе модель
добавит контактное пятно сама, а вырезание фона его не снимет: тень темнее
фона, заливка от края её не берёт, а снятие тени объедает нижнюю кромку самого
предмета. Оплачено дважды — лист гаек пришлось перерисовывать.

### 9.3. Чем нарезать и вырезать фон

Нарезка листа — PIL, по числу колонок и рядов (готовый рецепт лежит у соседей
в `words-chat/`, лист `words-thumbs-sheet-7x8.jpg` и папка `thumbs-cut`).

Вырезание фона — **скилл `bg-cutout`**, не руками:

```
python scripts/cutout.py "art/*.png" --out art/cut/ --contact-sheet
```

- По умолчанию `rembg` моделью `isnet-anime` — она под рисованное с обводкой.
- 🔴 **Контактный лист обязателен.** На нём брак виден сразу; без него бледный
  предмет уезжает в игру пустым.
- Бледный предмет пропал → `--method colorkey` (заливка от края; на плоском
  фоне точна независимо от бледности).
- Серый призрак тени → `--flatten-alpha 170`.
- Плотная тень пережила → `--strip-offset-shadow`.
- ⚠️ На светлом предмете (мятный, белый) заливка с числовым допуском съедает
  низ — у скилла допуск считается от самого изображения, поэтому зови скилл, а
  не свой скрипт.

### 9.4. Куда класть готовое

Вырезанные `.webp` — в `frontend/assets/images/<твой раздел>/`, карта
`require()` — **сгенерированным** файлом (`*.generated.ts`) со скриптом
сборки рядом. ⚠️ Шапка «собрано скриптом» обязана называть СУЩЕСТВУЮЩИЙ
скрипт: 06.09.2026 карта гаек звала `build-nut-styles.mjs`, которого не было,
и следующий заход правил бы «сгенерированный» файл руками.
