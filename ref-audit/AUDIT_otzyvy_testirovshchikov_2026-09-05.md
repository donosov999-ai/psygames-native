# Разбор отзывов PsyGames · app_feedback · снято 05.09.2026

Источник: Supabase `personal-nzt` (`iuvvheeocobhiothfgei`), таблица `app_feedback`.
Код сверялся в `/Users/denisonosov/dev/psygames/frontend`, версия исходника **2.38.3**
(`package.json:version`, коммит `b05435d1`). Ничего не менял — только SELECT и чтение файлов.

---

## 1. Срез цифрами

**Всего 402 отзыва**, 17.07.2026 → 05.09.2026.

### По статусам

| статус | всего | из них робот | с расшифровкой |
|---|---|---|---|
| `fixed` | 309 | 68 | 82 |
| `robot` | 57 | 57 | 18 |
| `new` | 18 | 0 | 0 |
| `done` | 15 | 0 | 5 |
| `in_progress` | 2 | 0 | 0 |
| `wontfix` | 1 | 0 | 0 |

⚠️ **Расхождение с постановкой задачи.** В задаче предполагались статусы `new`/`in_progress`/`done`.
На деле закрытая куча называется **`fixed`** (309 строк), а `done` — это старая закрытая куча из
июля-августа (15 строк, последняя 07.08). Формулировка «читай всё, кроме `done`» дала бы 387 строк,
из которых 309 уже закрыты. Читал по смыслу: **всё, что не `fixed` и не `done` — 78 строк**.

Куча `fixed` действительно закрыта, а не помечена задним числом: **у всех 309 строк заполнены
и `fixed_in_version`, и `fix_note`** (замер: `count(*) FILTER (WHERE status='fixed' AND fix_note IS NOT NULL)` = 309).

### По платформам

| платформа | n |
|---|---|
| tauri-android | 275 |
| tauri-ios | 66 |
| web | 54 |
| ios-sim | 3 |
| tauri-desktop | 2 |
| ios | 2 |

### По типу и автору

- `kind`: confusion 254 · bug 111 · idea 37
- `person`: Гость 135 · Релакс 128 · NZT-48 54 · Pro 23 · ODV999 14 · остальные 12 профилей по ≤12

### Голосовые — где была ловушка и как цифры выглядят на самом деле

Ловушка подтвердилась: у голосовых `message` = заглушка `[голосом, без текста]`, текст лежит в `transcript`.

- строк с заглушкой в `message`: **139**
- из них с расшифровкой: **98**
- без расшифровки: **41**

Это и есть «138/98» из постановки (точное число — 139).

🔴 **Но эти 41 «потерянных» — почти целиком робот, а не люди.** Разбивка:

| | строк с заглушкой | без расшифровки |
|---|---|---|
| робот (`is_robot=true`) | 125 | **39** |
| человек | 14 | **2** |

То есть у людей расшифровано **13 из 14** голосовых, потеряна одна открытая (`ff89b158`, picture-pairs,
03.09) и одна в уже закрытой куче. **Системной потери голоса нет** — 39 пустых принадлежат
Google Play pre-launch-боту, у которого микрофона нет вовсе (его собственная расшифровка:
«не голос человека: автообход Google Play pre-launch на эмуляторе — микрофона нет»).

### По экранам (открытые кучи)

Открытые 78 строк: 57 `robot` (все — бот на `tauri-android`), 18 `new`, 2 `in_progress`, 1 `wontfix`.

---

## 2. Четыре кучи

Разбираю **21 строку** — `new` (18) + `in_progress` (2) + `wontfix` (1).
Отдельно 57 строк `robot` вынесены целиком в мусор, см. ниже.

### (а) МУСОР — 11 + 57

**Признак:** текст — обрывок англоязычного автодополнения клавиатуры iOS, отправленный случайным
нажатием. Ни одного русского слова, длина ≤ 7 символов, `transcript` пуст, скриншота нет.

Ровно **11** штук, число из задачи подтвердилось:

| id | экран | текст |
|---|---|---|
| `51373cb1` | math-sprint | `I` |
| `750f1c08` | / | `The` |
| `d7be0348` | hanoi | `I` |
| `68dda52d` | reading-span | `I don't` |
| `8998b436` | picture-pairs | `I` |
| `f6b8751f` | / | `I have` |
| `66f37036` | sudoku-hub | `I` |
| `6a716789` | /statistics | `I don't` |
| `4d028905` | / | `I have` |
| `a998da26` | /leagues | `I` |
| `e9acafcb` | / | `I` |

Все 11 — `platform=tauri-ios`, 03–05.09. Похоже на один и тот же промах: поле отзыва ловит
предложку iOS-клавиатуры и уходит по кнопке до того, как человек что-то напечатал.

**Плюс вся куча `robot` — 57 строк.** Признак: `is_robot=true`, `message` = заглушка,
`transcript` либо пуст (39), либо прямо говорит `[микрофон не отдал звук — цифровая тишина]` /
`[не голос человека: автообход Google Play pre-launch на эмуляторе]` (18). Прочитал все 57 —
**содержательного текста нет ни в одной**. Это шум предзапускового обхода Google Play.

**Отдельно, не мусор, но и не читаемо — 1 строка:** `ff89b158` (03.09, picture-pairs,
tauri-android, `kind=bug`) — заглушка `[голосом, без текста]`, `transcript` пуст, `audio_path` пуст.
Живой человек что-то сказал, и запись не доехала. Чинить нечего, но и разобрать нечего.

### (б) УЖЕ ПОЧИНЕНО — 4 позиции

Проверял **чтением кода**, не по датам.

| что | где видно, что починено |
|---|---|
| **«не понятно по смыслу игры»** (половина отчёта `afa77c5a`, memory-palace) | `src/games/memory-palace/MemoryPalaceGame.tsx:619-628` — комментарий прямо цитирует этот отчёт («Отчёт NZT-48: „нихуя не понятно ни по смыслу игры"»), задача 9421ebcb; добавлена строка `strings.purpose`, которая рисуется в фазах `route` и `place` **до** механики. Вторая половина отчёта НЕ починена, см. живые дефекты. |
| **Вертикальный «уезд» экрана при протаскивании пальцем** (половина отчёта `e35e3171`, dots-connect) | `src/components/GameShell.tsx:651-652` — `touchAction: 'none'` + `overscrollBehavior: 'none'` на контейнере поля. Проверил, что dots-connect эту ветку получает: `app/games/dots-connect.tsx:267` использует `GameShell` **без** пропа `scrollableField` (список `scrollableField` в `app/games/*.tsx` его не содержит), значит идёт в ветку с запретом прокрутки. Горизонтальный «уезд» этим НЕ лечится, см. живые дефекты. |
| **«с десятой попытки не пускает на следующий уровень»** (dots-connect, 05.09 06:38) | Отдельная строка, уже `status=fixed`, `fixed_in_version=2.38.1`, `fix_note`: «Решил доску — прошёл уровень, сколько бы попыток ни ушло. Требование аккуратности ≥0,8 убрано из допуска». |
| **«откуда эта полоса вылезла… сколько пусто между разделами»** (`d2375dbe`, главная) | Статус `wontfix`, `fixed_in_version=2.37.42`, в `fix_note` стоит замер: «на твоём же вьюпорте 403×873 горизонтальных линий высотой 1–3 px и шириной больше 100 на главной НОЛЬ; пустота между разделами 15, 15, 23, 25, 25, 25 точек». Отчёт снят на 2.37.13, каталог с тех пор перестроен. Замер есть — позицию считаю закрытой. |

### (в) ЖИВОЙ ДЕФЕКТ — 3 позиции

Разбор с подтверждением — в разделе 3.

1. `e35e3171` — dots-connect, экран ездит вбок пальцем
2. `afa77c5a` — memory-palace, игра не помещается в экран
3. `291c2cff` — число на значке развилки не сходится с тем, что внутри

### (г) ПОЖЕЛАНИЕ — 5 позиций

Признак: просьба о том, чего нет, а не поломка существующего. Все `status=new`, все от NZT-48, 04–05.09.

| id | экран | дословно | толкование |
|---|---|---|---|
| `d3423751` | attention-conflict | «Надо прокачать их , каждому продумать уровни сложности а потом замиксуем в уровни как таблицы шуток или в зарядке» | Просит лестницу сложности для каждой пробы конфликта внимания и потом сборку их в уровни. |
| `a0d1574f` | digit-span | «Надо добавить вариант когда цифры сразу показываются на экране, а так получается что это последовательно долго ждать это утомляет немножко по-разному в памяти работает» | Просит второй режим предъявления — все цифры разом, а не по одной. Это отдельный конструкт памяти, а не ускорение. |
| `64e7cadf` | eye-gym | «Надо ещё добавить чтобы шарик увеличивался и уменьшался в размерах, ну то что типа он сужался в точку и чуток расширялся … чтобы глаз соответственно мог перефокусировать» | Просит тренировку аккомодации: шар меняет размер, глаз перефокусируется. |
| `68be1f3e` | eye-gym | «Надо чтобы туда таймер когда закончится вибрация шла … лучше сенсорный отклик в начале и в конце» | Просит виброотклик на старте и финише таймера. (Расшифровка местами битая — «ротами чики воид», — но начало и конец фразы читаются однозначно.) |
| `37fb4cef` | eye-gym | «Добавить помимо прямых движений, добавить волнообразный спирали образные движения, чтобы они были не такие прямые предсказуемые» | Просит непрямые траектории движения шара. |

Три из пяти — про eye-gym, поданы в течение двух минут (10:18–10:20 05.09). Похоже на один заход
с прицельным разбором одной игры.

---

## 3. Живые дефекты — по убыванию цены для человека

| # | что ломается | где | чем подтверждено | цена |
|---|---|---|---|---|
| 1 | Экран уезжает вбок от пальца прямо во время соединения точек — жест игры и прокрутка документа спорят за одно касание | `/games/dots-connect`, tauri-ios, 2.37.54 | Отзыв `e35e3171` (05.09 06:37, `in_progress`): **«Экран все ещё сдвигается пальцем влево вправо, это очень мешает игре особенно когда соединяешь точки»**. Слово «всё ещё» — потому что 02.09 был отчёт про то же (`fixed` в 2.34.0: «Окно ездит когда начинаешь их соединять через DragandDrop»), и правка не помогла. `scripts/pan-audit.mjs:9-22` разбирает почему: у «уезда вбок» две причины, `touch-action` лечит только вторую, а первая — страница шире окна — живёт выше по дереву. Собственная пометка разработчика в `fix_note`: **«ЧАСТИЧНО, нужна перепроверка на телефоне… САМО ПОВЕДЕНИЕ НА iOS Я НЕ ВОСПРОИЗВЁЛ»** | **Ломает партию.** Игра целиком про протаскивание пальца по сетке; пока экран едет, провести линию нельзя. Репортёр на tauri-ios (WKWebView), а гейт зелёный в headless-хроме — то есть именно на том движке, где жалоба, проверки не было. |
| 2 | В фазе размещения предметы и сам «дворец» стоят друг под другом в одном ScrollView — чтобы связать предмет с местом, надо прокручивать вверх-вниз на каждый шаг, и так всю партию | `/games/memory-palace`, tauri-ios, 2.32.0 | Отзыв `afa77c5a` (02.09 11:52, `in_progress`, **`fix_note` пуст — за 3 дня не тронут**): **«надо бегать вверх вниз чтобы что-то с чем-то совместить по сути между двумя страницами бегаешь целую игру»**. В коде порядок узлов в фазе `place` ровно такой: `src/games/memory-palace/MemoryPalaceGame.tsx:638` — `<View style={styles.itemGrid}>` с плитками предметов, затем строка выбора, и только на `:676` — `<PalaceScene>`, внутри которого места (`styles.lociGrid`, `:362`). Плитки `itemChoiceCompact` — 76×76 (`:769`), сетка переносится (`flexWrap`), сверху ещё шапка, строка смысла и текст фазы. | **Делает игру изнурительной от начала до конца.** Не теряет данные и не роняет партию, но цена платится на каждом из `lociCount` ходов, а не один раз. Вторая половина отзыва («не понятно по смыслу») уже закрыта, эта — нет, и по ней вообще не начинали. |
| 3 | Число на значке развилки не сходится с числом карточек внутри: значок считается по полю `mergedInto`, а экран развилки рисует свой, руками написанный список маршрутов | главная `/`, tauri-ios, 2.37.54 | Отзыв `291c2cff` (05.09 09:26, `new`): **«В хабах неправильная количество вложенных игр указано упражнении, то есть написано например один а по факту там два стоит и так абсолютно во всех профилях особенно в МЗТ»**. Замер по текущему исходнику 2.38.3 (`scratchpad/hubcount2.mjs`): **расходятся 6 развилок из 16**. Точный разбор самой чистой: значок «Зрительной памяти» = **2**, внутри карточек **3**. Почему — `app/games/visual-memory-hub.tsx:25-27` перечисляет memory-matrix, picture-pairs, navigator, а `mergedInto: 'visual_memory_group'` стоит только у двух (`src/constants/games.ts:453` и `:543`); у `memory_matrix` родитель другой — `src/constants/games.ts:442`, `mergedInto: 'span_group'`. Значок берётся из `app/index.tsx:448-465` (`составРазвилки`, счёт по `mergedInto`), карточки — из `src/components/HubScreen.tsx:86` (`visibleSuiteCards` по списку из файла развилки). | **Обещание, которого экран не выполняет** — ровно то, о чём предупреждает комментарий в `app/index.tsx:452-456`. Партию не ломает и данные не теряет, поэтому третья; но врёт «абсолютно во всех профилях», то есть попадается всем и постоянно. |

### Полный список расхождений по значку развилки (исходник 2.38.3, без фильтра профиля)

| развилка | значок | карточек внутри |
|---|---|---|
| `span_group` | 7 | 6 |
| `sudoku_group` | 4 | 5 |
| `attention_conflict` | 14 | 8 |
| `inhibition_group` | 4 | 2 |
| `visual_memory_group` | 2 | 3 |
| `languages_group` | 11 | 2 |

Сходятся 10 из 16. По `languages_group` расхождение **намеренное** — комментарий
`app/index.tsx:441-445` объясняет, что зонтик разворачивается вглубь до упражнений, и значок
показывает 11 занятий при 2 вложенных меню. Остальные пять — нет.

Корень один: **у игры один родитель (`mergedInto`), а появляться она может в нескольких развилках.**
`memory_matrix` живёт и в `span`, и в `visual-memory-hub`, но в счёт попадает только к `span_group`.

---

## 3-бис. ⚠️ Немой микрофон — дефект настоящий, но его чинят прямо сейчас

Пока разбирал, в рабочем дереве обнаружились НЕзакоммиченные правки другого агента:
`frontend/src/services/voiceNote.ts` (+154 строки), `appFeedback.ts` (+15), `FeedbackWidget.tsx` (+13).
Читал только диф, ничего не трогал. В комментарии к правке стоит замер:

> «у 32 из 34 нативных заметок в базе стоит `audio_measured: false` … Тридцать два отзыва уехали
> с „спасибо" при пустом звуке. Замер 05.09.2026, четыре скачанных из бакета `.m4a`,
> `ffmpeg -af volumedetect`: у всех четырёх `mean_volume = max_volume = −91,0 дБ` —
> цифровая тишина при валидном AAC 16 кГц моно».

**Как это сходится с моими цифрами.** Мой замер по базе: расшифровок со словами «тишина» /
«не отдал звук» — **84, и все 84 принадлежат роботу** (`is_robot=true`); среди людей таких **0**.
То есть механизм поломки реальный и общий, но в записанных данных он пока съел робота, а из живых
людей — только одну заметку (`ff89b158`). Противоречия между двумя замерами нет: «32 из 34» — это
срез по НАТИВНОЙ платформе, мой — по человек/робот.

Вывод для приоритета: **отдельным живым дефектом это не завожу** — по нему уже идёт работа в дереве.
Но если правка не доедет, цена вырастет: человек нажимает «отправить», видит «спасибо» и уходит
уверенным, что отзыв доставлен, а доставлена тишина.

---

## 4. Что я НЕ проверял и почему

- **Гейт `scripts/pan-audit.mjs` не запускал.** Ему нужен собранный `dist` и поднятый сервер
  (`scripts/pan-audit.mjs:28-31`), то есть сборка. В репозитории параллельно работают другие агенты,
  и задача запрещает менять что-либо — сборка перезаписала бы `dist`. Поэтому по дефекту №1 опираюсь
  на замер, уже записанный разработчиком в `fix_note`, и на то, что он сам поставил `in_progress`,
  а не `fixed`.
- **Куча `fixed` (309) построчно не читалась.** Замер, по которому считаю её закрытой:
  у всех 309 заполнены `fixed_in_version` и `fix_note` (0 строк без них).
- **Куча `done` (15)** исключена постановкой задачи.

---

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная*
