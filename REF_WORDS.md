# REF_WORDS — наши словесные упражнения против пяти чужих игр

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная*
Замер снят 06.09.2026, 06:47–07:07, на `main` (`1111e4e`), машина MAC. Все проценты ниже получены
прогоном кода, а не чтением комментариев; скрипты и команды названы по месту.

⚠️ **ФАЙЛЫ ДАННЫХ ПЕРЕПИСЫВАЮТСЯ ПРЯМО СЕЙЧАС, ДРУГИМ АГЕНТОМ.** За час замера
`allWordsRu.json` пересобрался дважды (739 → 725 в 06:54 → 724 к 07:07), `allWordsEs.json`
1808 → 1654, и в `core/allWords.ts` подключились пять новых языков (06:56).
Проценты ниже сняты на срезе 06:58 (RU 725, EN 2000).

**Что от этого НЕ зависит:** доли укладки. RU дал 100 % свободно и 93,9 % в коробке 11×11 и на
739 раскладках, и на 725 — вывод устойчив к пересборке данных.
**Что зависит:** абсолютные числа уровней. Пересчитываются одной командой из §0.

---

## 0. Чем мерил

| Что | Чем | Команда |
|---|---|---|
| Кроссвордная укладка | `wordlist-build/probe_crossword.py` (написан для этой задачи) | `wordlist-build/.venv/bin/python3 wordlist-build/probe_crossword.py --locales ru,en --restarts 24 --fit 11` |
| Диагонали, размеры сетки филвордов | НАСТОЯЩИЙ генератор `src/games/fillwords/core` через jest | временный тест в скретчпаде, `npx jest --rootDir=frontend --roots=<scratch>` |
| Пулы слов, число колец | тот же jest-прогон | — |

⚠️ Комментарии в коде я не считал доказательством ни разу: и «восемь сторон», и «5×5 → 8×8»
проверены прогоном.

---

## 1. Что у нас уже есть (списком)

### 1.1 Анаграммы — ТРИ режима в одной игре
Переключатель: `frontend/app/games/anagrams.tsx:135` — `'classic' | 'square' | 'all'`.

| Режим | Файлы | Поле | Слов за уровень | Откуда слова | Лесенка | Подсказки |
|---|---|---|---|---|---|---|
| Классика | `app/games/anagrams.tsx:84-90` | круг букв + лента набора | 1 загаданное × 10 попыток | `src/data/anagrams-words.ts` + `constants/anagramWords.json` + `TRANSLATION_VOCAB` | ЕСТЬ: длина 4→9 (`LEVEL_LENGTHS`, стр. 84), с L7 лимит на слово 90с→30с | открыть букву, счётчик `hintUses` |
| Слово-квадрат | `src/games/anagrams/core/ring.ts`, `WordSquareGame.tsx` | 5×5, 4 слова по периметру с общими углами (`СТОРОНА=5`, стр. 36) | 4 | пятибуквенные из пула филвордов (`ring.ts:125` `собратьКольца`) | НЕТ: `кольца[(level-1) % кольца.length]` — уровень это индекс | есть (`labels.подсказка`) |
| Все слова | `src/games/anagrams/core/allWords.ts`, `AllWordsGame.tsx` | круг из 7–8 букв + пустые клетки по длинам | 6–14 (медиана RU 10, EN 14) | `constants/allWordsRu.json` 725 раскладок, `allWordsEn.json` 2000, плюс de/es/fr/it/pt | НЕТ: `allWordsPack` берёт пак по остатку от уровня | ЕСТЬ: `подсказкаAllWords` — открывает букву самого КОРОТКОГО ненайденного |

Круг букв — общий на все три режима: `src/components/letterWheel/LetterWheel.tsx` +
`geometry.ts` (`точкиКруга:24`, `шагЛинии:98`), ведение пальцем и тап.

Замер числа колец (jest, `собратьКольца` по живому пулу):
`ru 471 · en 536 · de 51 · es 23 · pt 19`.

### 1.2 Филворды — режим корректуры
`frontend/app/games/proofreading.tsx:143` — `type TaskMode = 'letters' | 'fillwords'`.
Ядро: `src/games/fillwords/core/{generator,session,words,types,i18n}.ts`.

- **Сетка:** `generator.ts:275-280`, `size = Math.min(8, 5 + floor((n−1)/4))`, `rows === cols`.
  Прогон лесенки: `1:5x5 5:6x6 9:7x7 13:8x8 17:8x8 20:8x8 … 500:8x8`. **Потолок 8×8, достигается
  на 13-м уровне; дальше 500 уровней ничем не отличаются от 13-го.**
- **Слова:** 3–8 букв (`words.ts:39,41`). Поле разбирается ЦЕЛИКОМ — каждая клетка принадлежит
  ровно одному слову (`types.ts:13`, гейт `assertFullCoverage`).
- **Число слов:** 8×8 = 64 клетки ≈ 13 слов; замер 200 полей — 1984 слова, в среднем 9,9 на поле.
- **Откуда слова:** своего словаря НЕТ, берутся из `TRANSLATION_VOCAB` + `anagramWords.json`
  (`words.ts:35-36`). Пулы: `ru 628 · en 543 · es 182 · pt 182 · de 172`.
  Языки вычислены фильтром, не списком: `FILLWORDS_LOCALES = de, en, es, pt, ru`.
- **Диагонали:** ЕСТЬ, см. §5.
- **Подсказки:** 3 на уровень (`proofreading.tsx:150` `FILLWORDS_HINTS = 3`).
- **Список искомых слов:** ЕСТЬ, тумблером (`proofreading.tsx:232` `показыватьСлова`,
  отрисовка `:1263-1281`, найденное зачёркивается).
- **Серия:** отдельный вид — блоки со сменой правила на ОДНОМ поле (`services/proofSeries`).

---

## 2. Механика за механикой

| Механика | Есть у нас (файл:строка) | Есть у них | Что тренирует |
|---|---|---|---|
| Собрать слово из общего набора букв | `anagrams.tsx:84` классика | все пять | извлечение из словаря по буквенному ключу |
| Найти ВСЕ слова из одного набора | `core/allWords.ts`, 725 RU / 2000 EN раскладок (+5 языков) | Wordathlon, Море Слов, Zen Word | беглость порождения (fluency), самый чистый тест из всей группы |
| Круг букв, ввод линией | `letterWheel/LetterWheel.tsx`, `geometry.ts:98` | Море Слов, Zen Word, Wordathlon | ничего когнитивного — это ввод; снимает трение на 7 прицеливаний |
| Пустые клетки показывают длину | `AllWordsGame.tsx:116-136` | Море Слов, Zen Word | сужение пространства поиска — единственная встроенная подсказка |
| Слова по периметру с общими углами | `core/ring.ts:36,54` | Wordathlon | удержание пересечений: буква работает в двух словах сразу |
| **Кроссвордная сетка из тех же букв** | **НЕТ** | **Магия Слов** | то же, что периметр, но на 10–14 словах вместо 4 — рабочая память под связями |
| Поиск слов в поле букв | `fillwords/core/generator.ts` | Филворды, Поиск слов, Найди слова | зрительный поиск + узнавание |
| Диагонали в поле | `generator.ts:46` (8 сторон) | Поиск слов русский | добавляет 4 направления сканирования к 4 |
| Список искомых слов рядом с полем | `proofreading.tsx:232,1263` | Найди слова | переводит задачу с ПОРОЖДЕНИЯ на УЗНАВАНИЕ |
| **Вытянутая сетка (5×10, 9×16)** | **НЕТ** — `generator.ts:280` `rows === cols` | Филворды (5000 уровней) | форма поля меняет стратегию скана: узкое поле сканируется полосами |
| Поле разбирается целиком, без шума | `types.ts:13` | Филворды (жанр) | закрытие уровня по объективному признаку, а не по списку |
| **Лесенка длиной в тысячи уровней** | НЕТ: филворды 13 ступеней, «Все слова» и «Квадрат» — 0 | Филворды 5000 | — (это про удержание, не про тренировку) |

---

## 3. 🔴 КРОССВОРД — главный вопрос. Замер

**Вопрос:** сколько наших раскладок «Все слова» укладывается в связную кроссвордную сетку.

**Скрипт:** `wordlist-build/probe_crossword.py`. Правила укладки — классический criss-cross,
те же, что видны на скриншотах «Магии Слов»:
слово идёт по строке или столбцу без изломов · чужая буква принимается только совпав ·
клетка до первой и после последней буквы пуста (иначе на поле читается «КОТ»+«ОСА» = «КОТОСА») ·
пустая клетка слова не касается боком чужой буквы · каждое слово, кроме первого, пересекает
уже уложенное (отсюда связность по построению).

Укладчик жадный с 24 перезапусками (разный порядок слов) — значит все числа ниже это
**НИЖНЯЯ оценка**: настоящая доля не меньше измеренной.

### 3.1 Свободная сетка — ложится ли вообще

| | RU (725 раскладок) | EN (2000) |
|---|---|---|
| уложилось ЦЕЛИКОМ, все слова, сетка связна | **725 = 100,0 %** | **2000 = 100,0 %** |
| слов уложено | 7500 из 7500 = 100,00 % | 26 726 из 26 726 = 100,00 % |
| слов осталось вне сетки | **0 во всех раскладках** | **0 во всех раскладках** |
| сторона сетки: медиана / среднее / максимум | 9 / 10,45 / 21 | 14 / 14,40 / 25 |
| площадь рамки: медиана / максимум | 72 / 168 | 112 / 182 |
| пересечений на сетку (медиана) | 9 | 14 |

Распределение стороны, RU: ≤8 → 26 %, ≤9 → 52 %, ≤10 → 61 %, ≤11 → 70 %, ≤13 → 84 %.
EN: ≤9 → 7 %, ≤11 → 21 %, ≤12 → 33 %, ≤13 → 44 %, ≤14 → 55 %.

**Ни одного отказа на 2725 раскладках.** Это и есть ответ на главный вопрос: наши данные под
кроссворд подходят целиком, а не частично.

### 3.2 В коробке — что реально влезет в телефон

Свободная сетка в 18 столбцов формально «уложилась», а на экране 360 pt это клетка 18 pt.
Поэтому второй замер: укладчику запрещено вылезать за рамку ещё во время поиска.

| Коробка | RU (725): уложилось целиком | EN (2000): уложилось целиком |
|---|---|---|
| 9×9 | 465 = 64,2 % (слов 92,45 %) | — |
| 10×10 | 587 = 81,1 % (слов 96,98 %) | — |
| **11×11** | **681 = 93,9 %** (слов 99,16 %) | 1254 = 62,7 % (слов 95,52 %) |
| **12×12** | — | **1817 = 90,8 %** (слов 99,14 %) |
| 13×13 | — | 1983 = 99,2 % (слов 99,93 %) |

Хвост, когда не влезло: RU 11×11 — одно слово вне сетки у 30 раскладок (4,1 %), два у 9, три у 5.
EN 12×12 — одно слово вне у 143 (7,2 %), два у 34, три у 6.

Остальные пять языков (выборка 300 раскладок, 16 перезапусков, коробка 12×12):
`de 95,3 % · es 100 % · fr 94,7 % · it 96,0 % · pt 96,7 %`; свободная сетка — 100 % у всех пяти.

### 3.3 Пример, который можно проверить руками

`probe_crossword.py --show ru:12` — база **бабушка**, 7 слов, сетка 9×5, вне сетки нет:

```
· · К · ·      слова пака: ака, баба, бабка, баш, башка, каша, куш
· · У · ·      столбец 2: КУШ (строки 0-2) и БАБА (строки 4-7)
Б А Ш · ·      строка 2: БАШ · строка 4: БАБКА
· К · · К      столбец 0: БАШКА (строки 4-8) · столбец 1: АКА (строки 2-4)
Б А Б К А      столбец 4: КАША (строки 3-6)
А · А · Ш
Ш · Б · А
К · А · ·
А · · · ·
```

### 3.4 Проверка, что проверка не призрак

«100 %» стоит ровно столько, сколько стоит проверяющий. `проверить()` пересобирает поле с нуля,
читает ВСЕ цепочки длиной ≥2 по строкам и столбцам и требует, чтобы каждая была словом пака,
плюс обходит граф пересечений на связность. Три мутации:

| Мутация | Результат |
|---|---|
| снято правило «пустая клетка не касается боком чужой буквы» | поймано **119 из 120** раскладок |
| два слова поставлены в разных углах, без пересечения | поймано: «сетка распалась: 1 из 2» |
| к «КОТ» приписана «А» вплотную | поймано: «на поле читается посторонняя цепочка 'КОТА'» |

### 3.5 Что это значит для режима

- **Данные под кроссворд у нас уже лежат.** Ни одного нового словаря, ни одной новой лицензии:
  те же `allWordsRu.json` / `allWordsEn.json`, что кормят режим «Все слова».
- **Уровней сразу:** 681 RU в 11×11 + 1817 EN в 12×12 = **2498 готовых уровней на двух языках**;
  с пятью остальными (de/es/fr/it/pt, по выборке ~95 % от ~1900 паков каждый) — **порядка 11 000**.
  У «Магии Слов» этот запас и есть весь товар.
- **Раскладки можно ЗАПЕЧЬ, а не считать в рантайме.** Замер размера: RU ≈186 КБ, EN ≈514 КБ
  (текущие файлы 112 и 263 КБ). Порт укладчика на TypeScript тогда не нужен вовсе — в JSON
  добавляется `grid: [h,w]` и `put: [[слово, r, c, направление]]`.
- ⚠️ **Число перезапусков — это цена.** RU в коробке 11×11: 16 перезапусков дают 92,6 %,
  24 дают 93,9 %. При запекании перезапусков можно ставить 200 и добрать остаток.

---

## 4. Вытянутые сетки филвордов

**Где задаётся размер:** `frontend/src/games/fillwords/core/generator.ts:275-280`

```ts
export function fillwordsLevel(level: number): FillwordsLevelCfg {
  const size = Math.min(8, 5 + Math.floor((n - 1) / 4));
  return { rows: size, cols: size, maxWordLen, timeLimitSec: Math.round(size * size * perCellSec) };
}
```

**Потолок числом:** 8×8 = 64 клетки, достигается на 13-м уровне. Прогон
`fillwordsLevel(N)` для N = 1,5,9,13,17,20,30,50,100,500 → `5x5 6x6 7x7 8x8 8x8 8x8 8x8 8x8 8x8 8x8`.
Одна строка — и `rows === cols` навсегда.

**Что мешает 5×10 и 9×16 — три места, и только одно из них настоящее.**

1. **Сам генератор НЕ мешает.** Он принимает `rows` и `cols` раздельно (`FillwordsRequest`,
   `generator.ts:283`) и на прямоугольниках работает. Прогон:
   `5×10 → собрано, 10 слов, 50 клеток` · `9×16 → собрано, 30 слов, 144 клетки` ·
   `4×12 → собрано, 10 слов, 48 клеток`. Гамильтонов путь и `assertFullCoverage` не падают.
   ⚠️ `timeLimitSec` при этом посчитан как `size*size` — для прямоугольника надо `rows*cols`,
   иначе 9×16 получит бюджет как 9×9.
2. **Экран НЕ мешает.** `proofreading.tsx:511-515` считает клетку по ширине И высоте раздельно,
   а корректурный режим той же игры уже ходит на прямоугольниках: `levelParams` (стр. 158-159)
   даёт 8×8 → **16×12**. То есть высокие прямоугольники отрисовываются прямо сейчас.
3. **Мешает ширина экрана, и вот это уже настоящее.** `cellSize = max(22, min(widthBased, …, 72))`,
   `gridWidth = cellSize * cols`. При ширине 360 pt: `containerW = 336`, `336/16 = 21`, а нижний
   зажим 22 → `gridWidth = 352 > 336`, **вылезает на 16 pt**. 9×16 влезает начиная с ширины
   экрана ≈376 pt (iPhone 14 = 390 pt: `366/16 = 22`, `352 ≤ 366` — влезает).
   5×10 влезает везде: `336/10 = 33 pt` на клетку.
   Словарь тоже не мешает: 144 клетки набираются 30 словами, пул RU — 628 слов.

**Вывод:** «вытянутая сетка» стоит одной строки в `fillwordsLevel` плюс замена `size*size` на
`rows*cols`. Для 16 столбцов дополнительно нужен либо нижний зажим 20 pt вместо 22, либо
горизонтальная прокрутка поля.

---

## 5. Диагонали — замер, а не комментарий

Комментарий `generator.ts:45` обещает восемь сторон. Проверено прогоном настоящего генератора
(20 уровней × 10 зёрен = 200 полей, 1984 слова, 7536 шагов):

| | RU | EN |
|---|---|---|
| диагональных ШАГОВ | 2512 = **33,3 %** | 2512 = 33,3 % |
| слов хотя бы с одной диагональю | 1536 = **77,4 %** | 1536 = 77,4 % |
| полей хотя бы с одной диагональю | 200 = **100,0 %** | 200 = 100,0 % |

Числа RU и EN совпали до единицы — и это не сбой замера, а следствие устройства: маршрут и
разрез зависят только от зерна и размера поля, слова кладутся в готовые куски. Язык на геометрию
не влияет.

Разбор жеста тоже диагонали принимает — не по коду, а по прогону: `resolveTrace` на путях слов
с диагональными шагами вернул `{ok:true}` пять раз из пяти (ДВА, ЗОНТИК, БАЛКОН, ВОРОБЕЙ, АНАНАС).

**У нас диагоналей БОЛЬШЕ, чем у «Поиска слов русский»:** там слово читается по прямой в одном из
8 направлений, у нас линия ГНЁТСЯ — слово может свернуть в каждой клетке. Треть шагов
диагональные, и три четверти слов хотя бы раз сворачивают наискось.

---

## 6. Что брать, что не брать

### Брать

| Что | Почему числом | Цена |
|---|---|---|
| 🔴 **Кроссвордная сетка как режим «Все слова»** | 100 % раскладок ложатся свободно (0 отказов на 2725); 681 RU (11×11) + 1817 EN (12×12) готовых уровня, ~11 000 на семи языках; данные уже есть; раскладки запекаются в JSON (+186/+514 КБ) | новый экран отрисовки; укладчик можно НЕ портировать |
| **Вытянутые сетки филвордов** | генератор уже умеет (5×10, 9×16, 4×12 собрались); экран уже рисует 16×12 | одна строка в `fillwordsLevel` + `rows*cols` в лимите времени |
| **Лесенка выше 13-го уровня** | сейчас уровни 13…500 идентичны — это ноль роста на всём хвосте | ось «форма поля» вместо «сторона»: 8×8 → 6×11 → 7×12 → 8×14 |
| **Сортировка паков по трудности** | замер по четвертям набора: слов на раскладку RU 10,2 / 10,4 / 10,8 / 10,2, EN 13,4 / 13,3 / 13,5 / 13,3 — роста НЕТ. `allWordsRu.json` отсортирован по базе по алфавиту (проверено `bases == sorted(bases)` → True), то есть «уровень» = алфавитный индекс | пересортировать JSON по числу слов × средней длине |

### Не брать — и чем померено

| Что | Чем померил и что получилось | Почему «нет» | ⚠️ Что ВСЁ РАВНО переносится |
|---|---|---|---|
| Диагонали из «Поиска слов русский» | прогон генератора: 33,3 % шагов диагональные, 77,4 % слов, 100 % полей (`generator.ts:46`) | у нас их больше: линия гнётся в каждой клетке, а у них слово прямое | их ЖАНР — слова читаются по прямой. Это ДРУГОЕ упражнение (сканирование лучами вместо прослеживания змейки) и годится как отдельный тумблер «только прямые» — он делает уровень легче без потери честности |
| Список слов рядом с полем из «Найди слова» | `proofreading.tsx:232` + отрисовка `:1263`; тумблер `показыватьСлова`, найденное зачёркивается | сделано 06.09.2026, в тот же день | их подача: список СБОКУ, а не над полем — на 16 столбцах место над полем кончится. Плюс у них список сгруппирован по длине |
| Круг букв из Zen Word / Море Слов | `LetterWheel.tsx` + `geometry.ts`, используется во ВСЕХ трёх режимах анаграмм (`anagrams.tsx:734`, `WordSquareGame.tsx:163`, `AllWordsGame.tsx:147`) | сделано 06.09.2026 | у них круг ещё и ПЕРЕМЕШИВАЕТСЯ кнопкой — у нас порядок фиксирован зерном уровня (`allWordsLetters`, `allWords.ts:59`). Перемешивание вручную стоит одной кнопки и заметно помогает, когда взгляд «залип» |
| «Собери все слова» из Wordathlon | `core/allWords.ts` + 725/2000 раскладок; пустые клетки по длинам `AllWordsGame.tsx:116` | сделано 06.09.2026 | у Wordathlon есть НАГРАДНОЕ слово-бонус (нашёл лишнее — идёт в копилку). У нас `сдатьСлово` возвращает `'мимо'` (`allWords.ts:84`) и слово пропадает. Копилка бонусных слов — дешёвая и заметная добавка |
| 5000 уровней «Филвордов» | `fillwordsLevel(N)` для N до 500 → все ≥13 дают 8×8 | у нас уровни бесконечны (поле генерится), но НЕ различаются | их 5000 — это не 5000 полей, это 5000 РУЧНЫХ раскладок с ростом. Переносится сама идея: параметр, который продолжает расти после того, как сторона упёрлась в экран |
| Периметр 5×5 из Wordathlon | `ring.ts:36`, колец ru 471 / en 536 | сделано 06.09.2026 | — (взято целиком, включая поправку «банк это плитки, а не разные буквы», `ring.ts:54`) |

### Готовое со стороны — рассмотрено и отклонено

| Что | Чем померил | Почему «нет» | ⚠️ Что ВСЁ РАВНО переносится |
|---|---|---|---|
| **npm `crossword-layout-generator`** (github.com/MichaelWehar/Crossword-Layout-Generator) — готовый укладчик кроссвордной сетки | Лицензия проверена ФАЙЛОМ, не бейджем: `LICENSE` = MIT, «Copyright (c) 2018 Michael Wehar». В проекте отсутствует — `package.json` и `node_modules` пусты по запросу `crossword`. Вход — список слов, выход — `position`/`startx`/`starty`/`orientation`, то есть ровно наш результат | Своё уже работает и закрыто гейтом: `core/crossword.ts` + `anagrams-crossword.test.ts`, 10 проверок, включая доказательство подделкой (сдвинутое слово и разорванная сетка обязаны краснить проверку). Замер укладки тоже уже снят — 0 отказов на 12 185 раскладках семи языков. Менять рабочий код на пакет значит завести новую зависимость и новый риск ради того же результата, и заново доказывать правило «пустая клетка не касается боком чужой буквы» — у пакета своя эвристика, соблюдает ли он его, неизвестно | Находка сама по себе: если однажды понадобится укладка в другом продукте — брать её, а не писать заново. Ссылка здесь именно для этого. ⚠️ Замер лицензии годен на 06.09.2026 |

⚠️ Записано с замером, а не молча: «нашли готовое и не взяли» без числа читается как лень,
а с числом — как решение. Отклонено потому, что своё **измерено и закрыто гейтом**, а не
потому, что чужое плохое.

### Найдено попутно — и снято тем же замером

В 06:47 прогон `allWordsLocales()` вернул `en,ru`, хотя в `constants/` уже лежали de/es/fr/it —
я записал это как «7305 раскладок неподключены». В 06:56 перечитал файл: `core/allWords.ts`
импортирует уже семь языков (RU, EN, DE, ES, FR, IT, PT), появился и `allWordsPt.json`.
Находка протухла за девять минут — параллельный агент дописывал ровно это.
**Дефекта нет; замер годен только с отметкой времени.**

---

## 7. Одной строкой

Из пяти чужих игр четыре механики у нас уже есть, и три из них сделаны сегодня. Незанятая
одна — **кроссвордная сетка**, и она единственная из всего списка требует не кода, а данных;
данные оказались готовы: **100 % раскладок ложатся в связную сетку (0 отказов на 2725), 2498
уровней помещаются в телефонную коробку 11×11 / 12×12**, а с остальными пятью языками — около
11 000. Вторая по дешевизне — вытянутые сетки филвордов: генератор уже умеет прямоугольники
(5×10, 9×16, 4×12 собрались), мешает одна строка `rows: size, cols: size`.
