# ТЗ чату «Сортировки» — сортировка товаров и её семья

*Автор: Denis Onosov (ODV999) · ⚠️ Информация конфиденциальная · 06.09.2026*

> 🔴 **ПРАВИЛА — В `CHATS_RULES.md`, И ОН ГЛАВНЕЕ ЭТОГО ФАЙЛА.**
> Здесь они пересказаны для холодного старта; при расхождении верен
> `~/dev/psygames/CHATS_RULES.md`. Правило меняется ТАМ и только там — иначе
> девять копий разъедутся, а на молча расходящихся копиях проект уже обжигался.
>
> **Первым делом — §0 того файла:** регистрация в TeamOps (иначе тебя нет на
> доске и задачи не дойдут), своя папка со своим `PROJECT_REF.md`, и только
> потом код.


Ты ведёшь один раздел ВДОЛГУЮ. Это не разовая задача, а хозяйство: игры про
раскладывание предметов по ёмкостям с конечным местом.

---

## 1. Твои файлы

🔴 **ЗОНА РАСШИРЕНА 06.09.2026 РЕШЕНИЕМ ДЕНИСА.** Дословно: «все сортировки из
хаба с сортировками твои, и переливалка тоже твоя». До этого здесь стояло
«не трогаешь `src/games/water-sort/` — там работают другие»; это отменено.

```
frontend/app/games/goods-sort.tsx        frontend/src/games/goods-sort/**
frontend/app/games/cake-sort.tsx         frontend/src/games/cake-sort/**
frontend/app/games/water-sort.tsx        frontend/src/games/water-sort/**
frontend/app/games/ball-sort.tsx
frontend/app/games/nut-sort.tsx
frontend/src/__tests__/{goods-sort,cake-sort,water-sort,ball-sort,nut-sort}-*
```

⚠️ **Шарики и Гайки — тот же движок, что Переливалка:** оба экрана это
`SortGameScreen` из `app/games/water-sort.tsx`, своё у них только оформление.
Значит правка ядра переливалки задевает три игры сразу, и мерить надо все три.

**За пределы этого списка не выходишь.** Не трогаешь `src/games/anagrams/`,
`src/games/fillwords/`, `src/games/dots-connect/` — там работают другие.

📍 **Замер лестниц на 06.09.2026** (последний уровень, где значение ВЫРОСЛО,
прогон до 120): товары **L52** · торты **L63** · переливалка/шарики/гайки
**L33**. Худшая — переливалка: объём перестаёт расти уже на L13, дальше только
три оси-осложнителя, и все упираются к L33.

**Общий слой — через координатора**, сам не правишь:
`src/constants/games.ts` · `profiles.ts` · `hubContents.ts` · `helpMap.ts` ·
`whatsNew.ts` · `CHANGELOG.md` · версии · метки git. Новую игру ты делаешь
целиком, а в каталог и развилку её вписываю я — скажи, когда готова.

Словари — **только свои новые ключи**, чужие строки не трогаешь.

---

## 2. Правила работы — постоянные

- 🔴 **`git add -A` запрещён.** Только `git add <свои файлы>`. 06.09.2026
  из-за подметания дерева работа трёх сессий уехала в чужие коммиты.
- `git pull --rebase` перед правкой, `git push` сразу после коммита.
- Коммит после КАЖДОГО куска. Подпись `[Claude·MAC]`.
- Версию не поднимаешь, метку не ставишь, выпуск не собираешь.

**Каждая правка несёт три вещи:** замер ДО числом · гейт, краснеющий на
состоянии ДО · мутация в обе стороны.

**Лестницу прогоняй ЦЕЛИКОМ по уровням**, не сравнивай край с краем.

**Отказ требует такого же замера, что и согласие:** чем померил, что вышло,
почему это «нет», и что из отклонённого всё равно переносится.

⚠️ **Проверка, разбирающая ИСХОДНИК, — не проверка.** 06.09 проба препятствий
считала дебют по месту в массиве, а игрок видит УРОВНИ; после появления
фильтра порядок массива перестал равняться порядку уровней, проба зеленела и
пропускала настоящий дефект. Проверяй ИСПОЛНЕНИЕМ: прогони `levelCfg(L)` по
уровням.

**Разбор уже написан — читай, не мерь заново:** `REF_GOODS_SORT.md`
(§1 инвентарь · §4а прокрутка · §4б одноцветный уровень · §7 цели уровня).

---

## 3. Что уже сделано — не переделывай

Ниши на 1, 2, 3 и 4 товара · препятствия (закрытая, запертая, накрытый товар,
примёрзший ряд) · строгая укладка · скрытая информация · четыре вида цели ·
отмена без предела · подсказка из решателя · перемешивание · проверка
решаемости каждой раздачи · ниша-джокер (коммит `2aa68dc1`).

---

## 4. Очередь — сверху вниз

### 4.1. 🔴 «Торты» — ОТДЕЛЬНАЯ ИГРА (решение Дениса)
Cake Sort: собрать круг из ШЕСТИ секторов, а не три в ряд. Я предлагал сделать
режимом — Денис решил отдельной игрой, и это его решение, спорить не надо.

Что важно замерить, а не унаследовать:
- калибровка сложности `REF_PER_TYPE = 2.2` снята под тройки; для шестёрок она
  неверна — перезамерь A\*, а не переноси число;
- тарелок на экране больше, чем ниш: посчитай размер сектора при 360 точках
  ширины ДО того, как рисовать;
- очередь входящих тарелок внизу — у нас такого нет нигде, это новая сущность.

Игру делаешь целиком в своих новых файлах (`src/games/cake-sort/**`,
`app/games/cake-sort.tsx`, свои пробы). В каталог вписываю я.

### 4.2. Подвижная ниша
Ось записана в `PSYGAMES_MERGE_PLAN.md:469` как ★★ и не сделана. Работа
начата — доведи.

### 4.3. Уровень в одном цвете
Различать по форме при подавленном цвете. **Режимом уровня, а не набором:**
гейт `goods-sort-setpicker.test.ts:143` жёстко требует `alike === ['dairy']`.
Арт не нужен — 18 спрайтов уже в тёплой семье 16–50°, хватает на потолок в 11
видов. Замер §4б: дубля с нашими пробами на конфликт признаков нет.

### 4.4. Витрина на 2–3 экрана
Денис решил брать, хотя разбор §4а был против. **Возражения обязаны быть
закрыты, а не проигнорированы:**
- при 10 рядах товар ужимается до 18 точек — нужна мини-карта, чтобы структура
  была видна даром, а содержание вспоминалось;
- возвращается дефект «перетаскивание лагает» (`GameShell.tsx:630-655`, четыре
  отчёта за 02.09) — замерь FPS перетаскивания ДО и ПОСЛЕ;
- на 30–40 нишах перестаёт доказываться решаемость — значит уровни генерировать
  офлайн и вшивать, а не считать на устройстве.

---

## 5. Готово — это когда

По каждому пункту: число ДО и ПОСЛЕ · гейт, краснеющий на состоянии ДО ·
мутация в обе стороны · `npx tsc --noEmit` чисто · `npx jest goods-sort`
целиком зелёный · свой коммит.

Полный `npx jest` и `node scripts/lint-ratchet.mjs` — один раз в конце. Ратчет
обязан сказать «Долг не вырос»; вырос — чини СВОИ замечания, не поднимай
потолок.

---

## 8. Своя папка и свой `PROJECT_REF`

🔴 **Первое, что делаешь — заводишь себе папку.** Файлы раздела не сыплются в
корень: там уже 220+ файлов и найти ничего нельзя.

```
~/Downloads/Code claude/psygames/<ИМЯ>-chat/
    PROJECT_REF.md      ← твой журнал, ведёшь сам
    art/                ← листы картинок, нарезка, контактные листы
```

Соседи уже так живут: `words-chat/`, `sorting-chat/`. Прежде чем создавать —
загляни, нет ли уже твоей папки.

**Что в твоём `PROJECT_REF.md`** (образец — у соседей, форма та же):

- ★ строка на каждый заход: дата, подпись `Claude·MAC`, что сделано, каким
  ЗАМЕРОМ доказано. Числа обязательны: «было 4,2 клетки на путь, стало 6,4», а
  не «стало лучше».
- 🔴 отдельной строкой — каждый найденный дефект и то, чем он был НЕВИДИМ.
- 🚫 отказы: чем мерил, что получилось, что из отклонённого переносится.
- Открытые вопросы к Денису — списком, чтобы он отвечал одним словом.

⚠️ **`PROJECT_REF.md` в корне репозитория — не твой.** Он у координатора и
лежит в `.gitignore` как коммерческая тайна. Твой — в твоей папке.

---

## 9. Картинки: как генерировать и как нарезать

### 9.1. Правила рисования

- 🔴 **Реалистично.** Объём, материал, свет — даже мультяшному предмету. Плоские
  заливки Денис бракует.
- 🔴 **Сетка листа фиксирована: 7 × 8.** Не 4×5, не 3×3 — семь колонок, восемь
  рядов. Спрашивать не нужно, это решено.
- Есть образец — **описывай ПОЗЫ, а не внешность.** Модель и так видит, как
  предмет выглядит; описание внешности поверх образца ломает сходство.
- Геометрию задавай ЧИСЛОМ: «три кольца, зазор в четверть диаметра», а не
  «чтобы не сливались» — иначе модель сливает.
- ⚠️ Модель отказывается рисовать похожих на киногероев. Отказ выглядит
  техническим, причина правовая — переформулируй, не долби.

### 9.2. Чем генерировать

MCP `kie` → `generate_image` (Nano Banana 2). Один лист сеткой — дешевле и
ровнее, чем 56 отдельных вызовов: **одна генерация 7×8, потом нарезка**.

```
mcp__kie__generate_image
  prompt: "<сетка 7×8, ряд = состояние, ровный серый фон, объекты НЕ отбрасывают тень>"
  resolution: "4K"
  output_dir: "~/Downloads/Code claude/psygames/<ИМЯ>-chat/art"
```

🔴 **«Объекты не отбрасывают тень» — обязательный пункт промпта.** Иначе модель
добавит контактное пятно сама, а вырезание фона его не снимет: тень темнее
фона, заливка от края её не берёт, а снятие тени объедает нижнюю кромку самого
предмета. Оплачено дважды — лист гаек пришлось перерисовывать.

### 9.3. Чем нарезать и вырезать фон

Нарезка листа — PIL, по числу колонок и рядов (готовый рецепт лежит у соседей
в `words-chat/`, лист `words-thumbs-sheet-7x8.jpg` и папка `thumbs-cut`).

Вырезание фона — **скилл `bg-cutout`**, не руками:

```
python scripts/cutout.py "art/*.png" --out art/cut/ --contact-sheet
```

- По умолчанию `rembg` моделью `isnet-anime` — она под рисованное с обводкой.
- 🔴 **Контактный лист обязателен.** На нём брак виден сразу; без него бледный
  предмет уезжает в игру пустым.
- Бледный предмет пропал → `--method colorkey` (заливка от края; на плоском
  фоне точна независимо от бледности).
- Серый призрак тени → `--flatten-alpha 170`.
- Плотная тень пережила → `--strip-offset-shadow`.
- ⚠️ На светлом предмете (мятный, белый) заливка с числовым допуском съедает
  низ — у скилла допуск считается от самого изображения, поэтому зови скилл, а
  не свой скрипт.

### 9.4. Куда класть готовое

Вырезанные `.webp` — в `frontend/assets/images/<твой раздел>/`, карта
`require()` — **сгенерированным** файлом (`*.generated.ts`) со скриптом
сборки рядом. ⚠️ Шапка «собрано скриптом» обязана называть СУЩЕСТВУЮЩИЙ
скрипт: 06.09.2026 карта гаек звала `build-nut-styles.mjs`, которого не было,
и следующий заход правил бы «сгенерированный» файл руками.
